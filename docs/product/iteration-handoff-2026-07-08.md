# WePicTool 迭代交接笔记

**日期：** 2026-07-08  
**当前状态：** 阶段二 AI 分类已接入（2026-07-09），小程序 UI 已升级为底部三 Tab + 微信聊天预览结果页，下一步进入抠图与白底卡片生成。

## 当日完成和确认的事情

- 小程序 AppID 已配置：`wxf16280c0ba5c507d`。
- CloudBase 环境 ID 已配置：`cloud1-d0g1blfsde474b168`。
- 测试环境已可用。
- 当前功能可以上传图片。
- 当前功能可以批量保存/下载结果图。
- 当前版本仍是阶段一能力：使用 mock 分组，没有真实 AI 分类、抠图和白底生成。
- 已关闭 `wx.cloud.init` 的 `traceUser`，当前项目不做登录和用户追踪。
- `private_getBackgroundFetchData:fail` 判断为微信开发者工具/后台拉取相关提示，不是当前业务代码主动调用造成。

## 当前产品判断

现在项目已经从“不能跑”进入“可用的技术闭环”，并且 UI 已升级为更贴近微信真实发送体验的预览形态：

- 小程序采用底部三 Tab（首页 / 记录 / 我的）。
- 结果页改为微信聊天窗口预览风格，上衣 / 下装 / 鞋子以“合并发图”气泡卡片展示，点击可展开横向滚动查看全部图片。
- 用户可以提前看到“发给朋友后”的真实效果，增强分享动机。
- 图片只是被分组和保存，还没有真正变干净。
- 分组仍是 mock，不是 AI 判断。
- 结果图还没有抠图、白底、主体居中、统一比例。
- 用户能完成流程，但还没有得到“值得发给朋友参考”的最终穿搭素材。

## 下一阶段推荐目标

下一阶段建议合并推进“阶段二 + 阶段三前半段”：

1. 接入 AI 图片分类。
2. 增加低置信度人工确认。
3. 支持结果页改分类。
4. 选型并接入一个抠图 API。
5. 先生成基础白底图：去背景、白底、主体居中、可保存。
6. 保留单张失败不影响整批的兜底逻辑。

## 产品迭代优先级

### P0：分类可信度

- 从 mock 分组改为 AI 分类。
- 标签固定为 `tops`、`bottoms`、`shoes`、`other_product`、`daily`、`unsupported`、`uncertain`。
- `confidence >= 0.8` 自动分组。
- `confidence < 0.8` 让用户确认。
- 完整人物试穿图默认 `daily`，不进入白底人像链路。

### P0：出图质量

- 对 `tops`、`bottoms`、`shoes` 做抠图。
- 生成白底图。
- 主体居中，大小统一。
- 白色/浅色衣物需要轻阴影、描边或浅灰底兜底。
- 单张失败不阻断整批。

### P1：结果页轻编辑

- 改分类（已可用）。
- 删除单张。
- 单张重做。
- 保存本组 / 保存全部 / 按分组保存。
- 编号更明显，方便朋友反馈“上衣 1 + 下装 2”。
- 微信聊天预览风格的合并发图展示与展开交互。

### P1：微信发送引导

- 保存后提示“先发上衣组，再发下装组，最后发鞋子组”。
- 结果页直接模拟微信聊天窗口效果，降低用户想象成本。
- 每组不足 3 张时提示普通发送。
- 总图数够但每组都不足 3 张时，不暗示能形成微信叠图。

### P2：数据指标

后续至少记录：

- `task_created`
- `classification_completed`
- `category_changed`
- `task_completed`
- `group_saved`
- `result_saved`
- `share_guide_clicked`
- `retry_triggered`

## 2026-07-09 补充：阶段二已完成

以下任务已在 2026-07-09 会话中完成：

- AI 分类已接入：云函数调用 DashScope `qwen-vl-plus`，环境变量 `DASHSCOPE_API_KEY` 已配置。
- 低置信度图片（`confidence < 0.8`）在结果页显示「待确认」角标。
- 结果页「改分类」能力已实现，用户可通过 ActionSheet 手动移动图片分组。
- 分类提示词已优化，增加 few-shot 示例，减少商品图误判为 `daily`。
- 图片从云存储下载后转 base64 传给 DashScope，解决 `url error` 400 问题。
- 新增本地测试脚本 `scripts/test-dashscope.cjs`。

## ~~明天建议先做的事情~~（已过时，见下方 2026-07-09 补充）

> **历史记录：** 以下建议在 07-09 会话中已完成。

1. ~~准备 50 张真实测试素材。~~
2. ~~先验证 AI 分类效果，不急着接抠图。~~
3. ~~用 `docs/ai-workflows/classification-prompt.md` 里的提示词跑分类测试。~~
4. ~~如果分类稳定，再开始接结果页”改分类”。~~
5. ~~然后再进入抠图 API 选型和白底图生成。~~

## ~~明天继续时的第一句话~~（已过时）

> **历史记录：** 以下口径在 07-09 会话中已完成。

```text
继续 WePicTool 下一阶段迭代：先做 AI 分类方案验证和结果页改分类，不急着做复杂 UI。
```

## 2026-07-12 补充：阶段三抠图已接入 + UI 改版

以下任务在 2026-07-12 会话中完成：

**阶段三抠图接入：**
- 云函数接入 DashScope `qwen-image-2.0` 做抠图，提示词要求去除背景替换为纯白。
- 抠图结果上传到云存储 `matted/` 目录，前端可切换查看原图/白底图。
- 抠图失败的图片保留原图，不影响整批结果。
- 尝试引入 `sharp` 做后端白底卡片合成，但 CloudBase 不支持原生 C++ 模块（错误码 145），已移除。
- 白底卡片合成改为前端 Canvas 方案（待实现）。

**UI 改版：**
- 结果页 `pages/result` 恢复为白色微信聊天风格，底部新增「📱 微信发送预览」按钮。
- 新增 `pages/preview`：深色全屏微信聊天预览页，堆叠卡片 + 展开/收起 + 滑动切换。
- 底部模拟微信输入栏，真实操作栏提供「返回结果页」按钮。
- Vite 演示版同步更新：白色结果页 + 深色预览页，点击按钮切换。

**已知限制：**
- CloudBase 云函数不支持 `sharp`、`canvas` 等原生模块，白底卡片合成必须在前端完成。
- 抠图使用生成模型（qwen-image-2.0）而非专业分割 API，边缘质量可能不稳定。
