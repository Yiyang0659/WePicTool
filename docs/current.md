# WePicTool 当前状态

**最后更新：** 2026-09-12
**当前集成基线：** `main`
**当前功能分支：** `codex/ai-outfit-picker-flow`
**继续开发工作树：** 仓库根目录 `/Users/Zhuanz/Desktop/WePicTool`
**发布结论：** 当前功能分支尚未合入 `main`。renderer在线012、normal、100%默认流量；新版本013已部署为normal并配置定向 URL 参数灰度（`gray013=1`→013），012仍为默认稳定/回滚版本。真实 `wx.cloud.callContainer`、`/render-stack`、图片成图、`msg_sec_check` 与新增诊断日志尚未取得认证冒烟证据，因此未切流，生产 **NOT READY**。网络、CDN和GitHub未改。

## 当前阶段

2026-09-12 分支收口：按用户确认固定当前工作树为本地测试基线，包含本机编辑、手写、保存进度、审核诊断和已有 AI 模型配置改动；381/381 测试及语法、小程序预检、TypeScript、文档检查通过。私人开发者工具配置与 `.env*` 不提交。本次不合并、不推送、不部署、不切流；013真实链路、AI实际调用和双端真机仍待验收。当前审核边界是“编辑本地、最终成品审核”，包含保存/完成/微信成品预览，并非仅相册保存。

2026-09-12 保存浮层遮挡修复：进度浮层显示期间卸载原生预览Canvas（主预览、旧预览、手写草稿和手写板），关闭后重挂载；后台导出Canvas保留。覆盖审核、准备和相册保存阶段。真机视觉结果待复核，未改变云端配置。

2026-09-12 编辑页保存反馈：已按确认效果图实现粉紫动态弹窗，审核/成图等待动画、准备状态和相册真实张数进度；没有新增审核请求，云端45009和013验收结论不变。定向20/20通过，完整检查及真机边界见当日记录。

2026-09-12 17:08 定向灰度验收：在既有 `fun-card-renderer` 服务的发布单中配置 `TrafficType=URL_PARAMS`，`gray013=1` 定向到013，默认优先级保持012；未改变百分比流量（012默认、013比例均0），未新建服务、未改网络/套餐/微信配置、未clearQuota。由于开发者工具调试控制台无法执行真实 `wx.cloud.callContainer`，本轮没有产生可归属013的运行时请求；013日志仅有部署记录，`/render-stack`、图片成图、`msg_sec_check`及线上`errmsg/rid/traceId`仍未验证，未切换默认流量。

2026-09-12 15:05 部署预检：基于当前379/379通过的工作树，在既有 `fun-card-renderer` 服务下发布 `fun-card-renderer-013`。013为normal，CPU/内存1/2、端口8080、最小/最大实例0/5、VPC与012一致，环境变量键集合和值的脱敏哈希与012一致；未新建服务、未改网络/套餐/微信配置、未clearQuota。当前012=100%、013=0%，保留012回滚。三款镜像字体均HTTP 200，容器启动和8080入口配置已核对。由于当前终端未取得可用微信开发者工具认证会话，真实`wx.cloud.callContainer` `/render-stack`、图片成图、`msg_sec_check`及线上`errmsg/rid/traceId`日志未验证，按门禁不切流。

2026-09-12 01:35保存失败复核：只读CLS确认012在01:35:11、01:35:14仍WECHAT_AUDIT_45009。官方msg_sec_check文档定义为每日额度超限；未上架100次/天，上架通常200万次/天/4000次每分钟，当前AppID上架状态未核实。客户端补45秒总期限、审核不可用30秒防重复、失败标题不再谎称已生成、保留返回编辑；这些只修体验，不解除平台额度。未重置额度或部署。

2026-09-12 09:31额度与日志复核：同一AppID只读查询 `/cgi-bin/openapi/quota/get` 返回 `daily_limit=2000000`、`used=2`、`remain=1999998`、`rate_limit.call_count=1050000`、`refresh_second=60`，因此不能继续按每日额度耗尽解释。北京时间09:01–09:03的CloudBase全量tcbr日志共16条，只有1条 `stage=audit-start kind=final`：09:02:36，实例/Pod为 `fun-card-renderer-012-df5dc4647-vmp75`；同秒只有1条 `WECHAT_AUDIT_45009` 与1条503结束，无瞬时重复。静态检查确认预览（`/preview-stack`、`/preview-scene`）和最终（`/render-stack`）均可审核；当前 `ENABLE_FUN_OFFLINE_PREVIEW=true` 时后台预热只加载本地字体，不发预览请求。结果页无 `onShow` 自动重试，重试仅由用户点击；编辑页导出锁已存在，并补充完成按钮的导航防重复。新本地日志仅记录API、HTTP状态、时间、截断原始errmsg、rid、正文SHA-256和请求traceId，不记录token、AppSecret、openid或正文；未部署，历史日志不会补回原始rid/errmsg，不能宣称线上已具备新字段。本轮代码回归379/379通过。未clearQuota、未改网络与云资源。

2026-09-12：趣味字画本机规则候选、三字体轮廓预览及编辑已接入，不请求审核/renderer。字体11个异步分包，本机解码避免包内TTF读取权限错误。保存/完成/微信成品预览仍云端审核，45009导出阻塞未解除。验证见`docs/iterations/2026-09-12.md`；未部署、合并或修改云配置。

2026-09-11 23时审核排查最新：真实callContainer /preview-scene于22:55:24.566发起，490ms返回503 SAFETY_UNAVAILABLE；012容器22:55:26及22:40多条日志均WECHAT_AUDIT_45009，定位微信文本审核调用限制，而非字体/内容违规。SearchClsLog查询requestId=24cd3075-773e-4ff5-9638-a9113cdbe6e6（非成图请求ID）。新增客户端审核/限流失败暂停后台队列，手动成功探测恢复；未变更云端012。具体配额类型/额度/恢复时间需微信后台确认，当前云端成图仍被阻断。

2026-09-11 改字预览跟进：代码确认预热可占满两个请求位，且已有图时延迟400ms；现改为后台最多占一个位、当前编辑立即提优并取消额外延时，保留错误分类。截图失败的真实云端根因尚未确认，9442自动化未连接，不能把队列修正当作云端故障已解决。画布下保存按钮缩小、右侧蓝色；底部主操作不改。测试结果见当日记录。

2026-09-11 编辑导出/预加载最新：根据用户明确的按钮方案及预热要求完成分支实现。画布下保存当前页/整组，底部固定微信预览/完成；直接导出继续审核、编号、高清图链路。候选生成后封面→卡片→三字体后台预热，编辑优先选中方案，共享下载后的临时文件缓存，并发2/最多60项/15分钟。未知新组合首次仍需生成，未新增云资源或部署renderer；自动化与验收结果见当日迭代日志。真机保存权限、固定栏和网络实测尚未验收，不宣称上线。

2026-09-11 手写交互修订最新：用户确认后完成实施计划并编码。局部线段橡皮保留其余片段，一次手势可撤销；新2160工作区/1080视口，写画与平移分开，保存全部笔迹与视口，旧草稿1080不迁移；10档笔宽与renderer同步，普通装饰一键首尾移动且文字槽位保持。完整361/361测试、语法、TypeScript、普通/声明式发布预检通过。012已normal、100%、8080；提交requestId=8503f9c3-d606-414a-9b7d-827f2c14f619，网络检查unchanged=true。微信开发者工具CLI code10需重新登录；残留自动化连接只能读页面路径，页面数据/截图失败。012真实callContainer与真机手感未验证。下一步只需用户重新扫码登录并编译当前工作树后做轻量验收，不重配云环境。

2026-09-11 手写缺陷跟进：当前分支已卸载弹窗背后的草稿缩略Canvas、将手写画布移出滚动区并增加裁剪、修正已添加素材边框被覆盖的问题。356/356测试、62文件语法、普通预检、TypeScript通过；真机串层/滚动效果尚未复核，不宣称视觉验收完成。局部橡皮擦、更多笔宽、工作区平移与一键置顶/置底的修订设计见 `docs/superpowers/specs/2026-09-11-handwriting-ergonomics.md`，待确认后实施，未修改线上011。

2026-09-11 最新：已按用户确认保留画布，重排参考图操作区，实现独立手写面板、真实本机手写草稿库及可编辑/变换手写贴纸。草稿库与项目草稿隔离。转成既有strokes协议沿用011审核，真实/render-stack返回200及5张上传，无需重新部署。开发者工具连接恢复，新页及手写面板运行截图已检查；353/353测试、语法/普通预检/TypeScript通过。实际双指/手感和逐像素对照仍待验收，历史记录中的关闭/登录失败不再是最新状态。

2026-09-10 编辑页参考图改版：已核对现有WXML，新增待确认设计 `docs/superpowers/specs/2026-09-10-editor-reference-layout.md`。参考图的多份手写草稿与“生成贴纸”超出现有单项目草稿/直接笔迹；拟保留大画布并还原下方操作区，新增独立手写面板、真实本机草稿及矢量贴纸。等待用户确认画布位置和新增行为后写实施计划；本轮未改页面或部署。

2026-09-10 23:07 最新：用户重新登录后，真实wx.cloud.callContainer单卡/preview-scene返回200（1108ms），含蓝色笔迹的/render-stack返回200（2746ms、5张上传），项目funtext_1789052852108。已下载查看首张1080图，文字/装饰/笔迹完整。ENABLE_FUN_LOCAL_EDITOR现已true，允许含笔迹进入最终审核生成流程，回滚关闭时仍阻断。当前分支codex/ai-outfit-picker-flow正确；CLI重新加载正确项目路径却返回code10需登录，模拟器导航亦异常，因此客户端新界面未核实，已请用户手动编译。此最新记录替代前文开关关闭/云端待验结论；真机、手感和生产门禁仍待验收。

本轮最终本地检查349/349测试、60首方JS语法、普通/声明式发布预检、TypeScript、文档治理和diff检查通过。下一步只需恢复开发者工具登录后进行011单卡预览及含笔迹最终图真实调用，成功再开启本地交互开关并做页面冒烟；不能把自动化通过视为已经开放。

2026-09-10 22:47 续接：011已normal、100%流量、8080，镜像后缀fun-card-renderer-011-20260910222927，原网络配置未变。真实冒烟在wx.login阶段返回“需要重新登录,access_token missing”，未发到容器，不能归因为011渲染失败；已告知用户在微信开发者工具重新扫码。补齐已完成笔迹独立Canvas缓存、切卡/退出手写取消活动笔迹、点数上限提示，定向3/3通过。新功能开关false及手绘导出阻断继续保留，待登录恢复后验证并启用。

2026-09-10 本地交互实施中：用户已确认书面设计；候选整框点击/防误触/当前卡交接、单卡底图接口与有界缓存、独立Canvas装饰/笔迹、基础笔刷及最近一个本机草稿已加入功能分支。新开关ENABLE_FUN_LOCAL_EDITOR仍为false。348/348测试、60文件语法、TypeScript和声明式发布预检通过。真实本机微信img_sec_check对固定线条图返回HTTP200/errcode0；已复用令牌缓存增加上传前图片审核。原服务更新已提交requestId=dcaf4e8d-f5ba-4ae1-9ab1-bb546a0e73c8，提交后网络配置未变；构建和真实新接口/手绘成图待验证，不能宣称上线完成。

2026-09-10 编辑体验与自由手写：用户已确认“本地实时交互、云端字体与最终成图”方向并授权规划实现。书面设计见 `docs/superpowers/specs/2026-09-10-local-interactive-editor-design.md`，待书面边界复核后编码。本轮仅补设计：单卡底图缓存、本地装饰与受限笔迹、本机草稿、最终图片审核；跨设备草稿同步不纳入本期。当前代码没有自由手写，现有文本审核不覆盖手绘内容，图片审核能力必须验证后才能开放最终导出。未改在线010或网络配置。

2026-09-10 最新：fun-card-renderer-010已构建成功、normal、100%流量、8080，HTTPS审核启用。真实wx.login+wx.cloud.callContainer /render-stack返回200，三字体各一张中文PNG成功生成并上传（诊断项目funtext_cloudcheck_1789036977548）。网络配置未变。基本身份/审核/渲染/上传链路恢复；客户端主预览切换、真机、运行时出站抓包和正式生产门禁尚未验证，不能据此宣布小程序已发布。

2026-09-10 已提交原服务源码及HTTPS审核变量（UpdateCloudRunServer requestId=33b2d316-5fee-4d7d-9cc5-a69bf9aa3d17），构建上线待核实。部署脚本只提交EnvParam与DeployInfo；提交后网络/实例规格/端口未变。91/91定向及语法、普通/声明式发布预检、TypeScript通过。不代表成图恢复。

2026-09-10 可信调用补齐（分支、未部署）：共享客户端每次wx.login，HTTPS模式只使用code2session核验身份，忽略伪造OpenID/context；5秒超时、8并发及每实例120次/分钟登录核验限制，失败不渲染。真实开发者工具登录code经新模块核验成功，随后固定中文审核OK。91/91定向测试通过。部署阻塞：CUA两次连接超时；已安装CLI更新会强制提交AccessTypes，因此未用其改云端。需恢复浏览器连接或准备保留访问配置的部署路径，线上仍009。AppSecret已填，不再等待填写。

2026-09-10 用户填写AppSecret后，本机真实微信HTTPS验证通过：AppID匹配，stable_token返回200及7200秒令牌；开发者工具wx.login取得临时code，服务端换取身份后，新审核模块对固定测试文字返回OK。秘密、登录code、session_key、OpenID及token均未输出/落盘。此证据只证明凭据和新审核实现可用，不代表009容器已更新或其出站/入口安全通过。线上仍未部署，部署前可信入口门禁保持。

2026-09-10 用户确认后，分支已实现显式wechat-https审核模式：stable_token缓存/并发合并/非强制刷新、超时取消、2.0严格pass判定、header身份上下文传递。新增根目录.env.renderer.local秘密准备入口（Git忽略、权限600，不自动加载或部署）及Docker排除规则。默认cloud-sdk保持不变；没有更新009。等待用户填写AppSecret，以及启用前可信入口核验；不能将本地实现当成云端成图恢复。

2026-09-10 修复方案复核：用户截图确认微信云托管prod不是原环境、服务为空；不迁移。只读009配置未发现小程序AppSecret相关键。官方2.0审核要求openid/version/scene及result.suggest判定，现有代码不能只补token。已形成待确认方案docs/superpowers/specs/2026-09-10-renderer-wechat-auth-repair.md：保留原服务、显式微信HTTPS鉴权、缓存stable_token、严格审核结果；IP白名单和入口可信性是停止条件，尚未实施/部署。

2026-09-10 00:52 容器只读诊断：现有009实例的Webshell确认令牌文件 `/.tencentcloudbase/wx/cloudbase_access_token` 为ENOENT，CBR_ENV_ID存在，WX_API_TOKEN和WX_CLOUDBASE_ACCESSTOKEN均缺失；/proc/1/environ交叉检查同样结果。仅向现有stdout写入布尔值/错误码，未输出令牌、密钥或用户正文，未改源码/部署/网络。已定位直接缺口为微信审核令牌来源缺失；平台为何未下发（能力支持/授权配置）仍待确认，不能宣称已修复。

2026-09-10 密钥专项实测：从服务009配置在内存中解析配套凭据，使用同一CloudBase环境完成诊断文本文件上传和删除（SUCCESS）。腾讯云凭据至少具备此次存储上传/删除权限；不是微信审核令牌的替代品。SDK源码确认getWxCloudToken支持WX_API_TOKEN和云托管挂载文件回退，先前“SDK不适用容器”的猜测不能成立为结论。剩余需验证实际容器的CBR_ENV_ID识别、令牌文件存在/非空及云调用授权；当前仍不宣称成图恢复。

2026-09-10 00:32 密钥更新后复核：用户更新后在线版本为009、normal、100%流量、端口8080。00:31:48.629真实 /render-stack 在615ms返回503 SAFETY_UNAVAILABLE；00:31:50的009容器日志仍为 invalid wx openapi access_token。本次只输出服务状态白名单字段，未输出任何密钥。更新版本已生效，但腾讯云密钥完整权限/云存储成功尚未证明，微信审核鉴权问题未解决；不需要用户重复轮换来修复此业务错误。

2026-09-10 00:05复核：两处登录恢复，控制台小程序认证页明确显示目标AppID“授权成功”；真实 /render-stack 仍754ms返回503，容器原始错误仍 INVALID_WX_ACCESS_TOKEN，不能把授权完成当成审核链路已修复。优先核对 index.js:28 使用的云函数兼容OpenAPI路径是否缺少容器中的微信调用上下文。服务详情读取意外包含明文腾讯云密钥，已告知用户轮换，未写入文档。详见当日迭代记录。

2026-09-09 23:50 续接：用户报告小程序认证已处理（审核成功仍待真实验证）。针对控制台“已暂停/0 实例”，只读 API 确认服务 normal、008 占 100%、alwaysScale、MinNum=0/MaxNum=5。现有地址请求后，23:50:23 容器输出 listening on 8080，随后 GET / 在 172ms 返回业务 JSON 404 NOT_FOUND，确认现有容器已唤醒；未重新部署或修改配置。小程序调用另返回 access_token missing，故本次不证明审核/成图链路恢复。最小实例仍为 0，空闲后可能再次缩容；改为 1 的持续费用需用户确认。

2026-09-09 07:58（北京时间）继续验证：启动微信自动化再次返回 `需要重新登录（code 10）`，端口 51181 已开启但登录失效，未取得新的 callContainer 请求。上一轮最终全量测试 310/312，仍有两个启动子进程的 1500ms 期限失败；直接启动检查在约 5627ms 后按预期以状态 1 拒绝缺失环境配置，不能宣称全量检查通过。语法、TypeScript、普通/发布预检、文档检查与 diff 检查通过。等待微信开发者工具重新扫码登录后继续一次真实调用。

2026-09-08 最新排查：此前真实 `/render-stack` 调用出现 `102002 请求超时`。本轮服务部署命令返回成功；只读服务详情确认在线 `fun-card-renderer-008`，镜像后缀 `fun-card-renderer-008-20260908220501`、端口 8080、状态 normal。该版本审核等待上限 6 秒，最终渲染等待上限 20 秒，并捕获 HTTP 处理器拒绝；preview 渲染阶段尚无总期限，超时包装也不会取消底层上传，仍需后续核实，不能宣称已修复所有超时。最底层云端阻塞阶段及对应请求日志尚未取得。客户端另有确定缺陷：诊断改动遗漏 `renderedCandidate` 定义和过期请求检查，导致服务端成功后仍显示失败；已恢复这两段逻辑并通过原失败测试。用户选择服务端作为主预览方向已确认，但当前先排查云端错误，客户端 CDN 兼容逻辑仍保留。详细证据及验证见 `docs/iterations/2026-09-08.md`。

项目已从“继续堆新玩法”进入“统一外壳、共享导出、外部验收和主线收口”阶段。当前工作树同时包含：

- 穿搭整理、分层云换装、趣味字画 P2.1 规则闭环；
- 趣味字画 P2.2 AI 故事规划实验代码，尚未确认发布范围；
- 已确认并完成分支实施的趣味字画 P2.3：12 个案例、8 种规则玩法、7 套风格、3 种授权字感与聚焦编辑；
- 三玩法共用的 manifest、可见 `01…N` 序号、预览、串行保存和失败续存；
- 已确认的首页“双玩法选择 + 单一共享预览”；
- 已确认并完成分支实施的微信效果预览页仿真改版；
- 已确认并完成当前功能分支实施的 AI 穿搭空选图状态，以及分层换装四组单图手势预览和右侧竖向选择摘要；首页进一步明确“好友四叠搭配”与“自动分类去背”的产品边界，并轻微扩大右侧摘要栏；
- 已确认并在当前工作树实施“穿搭叠图”统一流程：首页只保留一个穿搭入口，同一工作台支持自己分层、AI 整理、混合追加、结果复核与待确认归类；
- 已按用户真机截图完成跟进：空部位虚线加号整块可直接添加该部位图片；首页穿搭示例空闲时单组随机顺序切换，用户触摸/箭头操作优先并暂停自动演示；
- 已按用户提供的 16 张项目自有图片，把旧穿搭案例替换为四套完整搭配；首页和工作台各部位均显示 4 个按套装序号对应的选择；
- 待确认的界面与页面模板基线 `docs/product/DESIGN_SYSTEM.md`。

2026-09-06 已把 `codex/unified-stack-export` 的完整状态提交为 `0f55a38`，并通过合并提交 `f52a8c8` 合入 `main`；合并前主线为 `22b7a14`。当前又从 `main` 建立 `codex/ai-outfit-picker-flow`。2026-09-07 三款 OFL 字体先发布到静态托管供客户端跨设备加载；随后按用户新决定，renderer 将相同固定字体迁入 `assets/fonts/`，Docker 构建显式检查三个文件，进程启动期对每个 Canvas 字体集合只注册一次，正常渲染不再读取 CDN。`fun-card-renderer-004` 已发布，状态正常、100% 流量、端口 8080；镜像为 `ccr.ccs.tencentyun.com/tcb-100050496765-lapk/ca-pcsdrzau_fun-card-renderer:fun-card-renderer-004-20260907212701`。平台变量仍仅为 `NODE_ENV`、`CLOUDBASE_ENV_ID` 和 `FUN_CARD_RENDERER_ACCESS_MODE`，没有字体 CDN 地址。公网仍为 `ENABLE`；本轮遵照用户指示不再操作 VPC、子网、NAT 或公网开关。真实 `callContainer`、云存储生命周期和最终导出证据仍未取得。

开发者工具已恢复登录并完成趣味字画代表性全链路检查：内置示例生成三套候选、候选独立滑动、内容/样式/装饰三面板、代表性风格/背景/配色/字感、文字修改、撤销/重做、装饰添加与拖动、结果重新渲染和微信效果预览均可进入。用户随后指出并已修正缩略状态、排序补位和文字弹窗 Canvas 遮挡；装饰层级文案也改为“往前显示 / 往后显示”。本轮又增加装饰独立色板：鲜明、浅色、透明三组各 8 色，只修改当前选中装饰，可恢复原色，并保持本地预览与云端渲染一致。开发者工具已确认色板框、三分类、透明棋盘底和画布即时变色。随后复现并修复本地字体服务停机造成的编辑画布空白：编辑页和候选页现在都会把字体不可用接入已有服务端低清预览降级；若服务端预览也失败，则保留项目并显示友好的重试状态，不再向用户暴露 `ECONNREFUSED`。本地 renderer 已恢复，开发者工具已确认编辑页手写内容重新显示。现有证据仍不能替代 375 px、真实双指、iOS、Android、真实相册和真实微信聊天验收。

| 能力 | 代码状态 | 外部证据 |
| --- | --- | --- |
| 首页双玩法共享预览 | `codex/ai-outfit-picker-flow` 已实现四组单图 swiper、四套完整穿搭案例、单组随机空闲演示、用户优先暂停、竖向选择摘要与统一“穿搭叠图”入口 | 自动化通过；开发者工具既有模拟页视觉通过，新案例与最新动画待真机复核，真实手势与双端尺寸待验收 |
| 穿搭叠图 | 当前工作树已把手动分层和 AI 整理合为同一项目；支持 AI 复核、原图/白底选择、只追加回填和待确认归类 | 逻辑自动化通过；AI、画质、相册授权、回填、导出和真实聊天待双端验收 |
| 趣味字画 P2.1 | `main` 已实现，规则路径和本地 Canvas 兜底有自动化；renderer 三字体已随镜像发布并启动期注册 | renderer v004 已正常运行；真实 `callContainer` 三字体中文成图、云存储和双端待验收，网络资源操作已暂停 |
| 趣味字画 P2.2 | 实验代码存在 | 发布范围、云函数、模型密钥、审核与真机均未确认 |
| 趣味字画 P2.3 | `main` 已实现 12 案例、8 规则玩法、7 风格、3 字感与聚焦编辑；renderer 正常路径使用镜像内固定字体，客户端 CDN 暂留回滚 | renderer v004 已上线；真实服务端预览/高清导出与 Android/iOS 客户端字体加载待验收 |
| 统一叠图导出 | `main` 已实现 | 相册排列、角标遮挡、失败续存和真实聊天待验收 |
| 微信效果预览改版 | `main` 已实现固定外壳、约 38vw 的微信紧凑叠图、普通/深色切换、同步补位循环手势、逐张展开和诚实保存动作 | 294/294 全量自动化；开发者工具已验证左右循环、展开三条独立消息及收起保持顶层；双端真机待验收 |
| 设计系统固定化 | 已形成 Draft v1.0 | 等待用户确认后才能写实施计划和重构 |

## 分支状态

- `main`：当前唯一继续开发与测试基线；已包含穿搭整理、分层云换装、趣味字画、共享首页、微信效果预览和统一导出，尚未部署或发布。
- `codex/ai-outfit-picker-flow`：当前功能分支；已实现首页四组单图跟手切换、单组空闲演示、统一穿搭入口、工作台手动/AI 双添加方式、AI 复核回填和待确认归类；renderer 本地字体变更后全量 309/309 自动化通过，尚未合入 `main`。
- `codex/unified-stack-export`：已于 2026-09-06 合入 `main`，保留为只读的集成来源和差异追溯点，不再继续追加功能。
- `codex/fun-text-stack-phase1`：趣味字画 P2.1/P2.2 来源分支，能力已被当前集成基线包含，不应再并行追加同类改动。
- `codex/layered-dressup-mvp`：分层云换装来源分支，保留历史和差异核对用途。
- `codex/layered-dressup-design`：较早的设计分支，不再作为继续开发基线。
- `codex/bigtext-handwrite`：旧大字滑卡技术实验，已由趣味字画替代；只保留可追溯的字体、渲染与贴纸技术资产。

仓库根目录当前检出 `codex/ai-outfit-picker-flow`；完成视觉与真机验收后再决定是否合入 `main`。现有来源工作树只用于历史追溯。

## 阻塞项

1. 首页、预览、三玩法统一导出尚未完成 iOS、Android、系统相册和真实微信聊天验收。
2. 个人主体小程序的 AI/深度合成审核范围未确定；P2.2 不能据实验代码直接进入发布。
3. renderer-012曾返回WECHAT_AUDIT_45009；09:31同AppID实际额度仍充足，不能定性为每日额度耗尽。原始errmsg/rid缺失，需013真实调用与诊断日志核实当前状态。早期008令牌问题为历史记录。
4. renderer 正常渲染不再依赖字体 CDN；客户端仍通过现有 CDN 执行 `wx.loadFontFace`，作为跨设备加载与回滚方案。VPC、子网、NAT 和公网关闭操作按用户要求暂停，不能把当前公网状态视为生产通过。
5. 代码已合入 `main`，但尚未完成 G1 双端真机与 G2 生产云端门禁，不能因为已经合并就直接上传发布。
6. 设计系统固定化尚待用户确认；确认前不能进行跨页面样式重构。

## 下一步

1. 在微信开发者工具和 375 px 真机检查当前分支：首页单组随机空闲动画是否自然、触摸后是否停止抢操作、工作台空部位整框选图、AI 空选图/处理/复核/回填、待确认归类，以及四部位真实相册选择。
2. 通过本轮视觉与手势验收后，把 `codex/ai-outfit-picker-flow` 合入 `main`；若尺寸不合适，只调整首页三文件，不改素材页与导出协议。
3. 优先只读核对微信应用与云环境关联及 OpenAPI 调用方式，提出 INVALID_WX_ACCESS_TOKEN 的最小修复；不绕过内容安全，不盲目重新部署。修复后仅做一次成图与上传冒烟。当前不操作 CDN/CORS/静态托管权限。
4. 用户确认 `docs/product/DESIGN_SYSTEM.md` 的三 Tab、首页四层、令牌和 T1–T7 模板冻结范围；确认后再写逐文件实施计划。
5. 恢复真实调用后再验证 preview/render 与云存储链路；48 小时生命周期仍待验收。VPC、子网、NAT、公网、存储资源创建、合并主分支和 GitHub 推送均保持暂停。
6. 在 iOS、Android 和真实微信聊天验证安全区、横纵手势、图片比例、普通/深色状态栏、`01` 封面、相册选择、授权拒绝、画质遮挡和失败续存。
7. 决定上线主体、P2.2 是否进入发布包及 `ENABLE_FUN_TEXT_STACK_ENTRY` 默认值；不纳入的能力从发布包或入口中关闭。
8. 若纳入趣味字画云端能力，按部署手册完成 Docker、CloudBase、私密入口、字体、清理周期和线上冒烟，并让发布模式预检通过。
9. 所有发布门禁通过后再上传体验版/审核版；实际发布后更新发布状态和版本变更摘要。
