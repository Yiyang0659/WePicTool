# 分组末尾添加入口（已确认）

2026-09-13 用户确认：AI仍自动归类、手动直入当前组、不清空已有用户图片。随后确认水印使用简短“AI”：通过官方footnote字段配置，编号仍由小程序随排序合成，不烘焙进模型图片，不改变模型通道。

## 需求与范围

每个已显示分组的横向素材列表末尾新增同缩略图尺寸的加号框，滑至末尾可见；空组原加号改为同一双选入口。其他素材仍仅有AI结果时出现，不为入口提前创建。保留底部已有按钮与其他布局。

点击显示“继续让 AI 整理”“自己添加图片”。手动添加直入所点分组；AI复用逐图处理与失败重试页面，仍按识别类别分组，不强制把鞋子/风景归为所点上衣组。此处路由语义需要用户确认。沿用每组12张限制；满组提示已满，保存/成图中不可增改；取消菜单无副作用。AI入口不清空用户已有素材，内置示例沿用原清空确认。

## 水印调查与已确认配置

本地hunyuanImage未传水印字段，cardComposer和sequenceBadgeComposer没有绘制“AI生成”。截图疑似供应商返回图自带，仍需原始输出图对照才能完全归因。CloudBase官方AI SDK文档对当前I2I提供footnote自定义文字（最多16字符），未明确提供关闭开关；其他腾讯接口LogoAdd不可直接挪用。腾讯混元FAQ要求保留人工智能生成标识，可调整内容。暂不裁切/涂抹水印，不改付费接口或额度来源；如优化标识应单独核实官方配置和真实输出。

参考：https://docs.cloudbase.net/ai/sdk-reference/api ，https://cloud.tencent.com/document/faq/1668/86251

设置footnote为“AI”，不传未经证实的关闭参数。不改变其他生成参数、重试、Key或额度来源。仅影响部署后的新生成图，历史图片不追溯处理；供应商实际标识外观仍待真实成图核验。

## 实施与验证边界

确认后编写实施计划，再补菜单路由/空满组/取消/保存状态测试，执行治理检查；真机横向尾卡与选择回填需要验收。本轮仅设计及只读调查，无模型请求或部署。
