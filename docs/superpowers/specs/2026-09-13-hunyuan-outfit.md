# 千问分类与混元成图

用户已确认：2026-09-13 要求接入混元并提供提交步骤。范围为现有 processOutfit，不新增服务、不修改 renderer 路由。分类固定 qwen3.8-flash，密钥只读服务端 DASHSCOPE_API_KEY；成图固定 hunyuan-image / HY-Image-v3.0-I2I-ToB-v1.0.1，通过官方 SDK 使用当前环境身份。不给混元传入阿里云 Key，不自动回退阿里云成图。

审核、分类、逐图成图、转存、前端构图与归组保留。生成失败不自动重试（超时可能已消耗额度），保留原图并标为处理失败。缺 Key 明确失败，不伪造分类。批量生成可能超过90秒，部署需核对函数/客户端等待期限；有限并发避免增长计划限流。无真实请求前不得宣称免费抵扣或质量通过。

依据：https://docs.cloudbase.net/ai/image-model/wx-server-sdk 。用户截图显示当前环境图生图开关开启、总额度100000、用量0、有效期2027-03-06；非本轮实时查询。云函数计算/存储费用不被生图赠送额度覆盖。
