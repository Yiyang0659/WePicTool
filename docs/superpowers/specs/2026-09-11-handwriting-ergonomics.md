# 手写板交互修订

状态：用户已确认，进入功能分支实施。

## 已定位与本轮修复

- 保存草稿后，背景草稿缩略图仍挂载原生 Canvas，可能串入浮层；打开手写面板时卸载缩略 Canvas，关闭时恢复，不删除草稿。
- 画布位于纵向滚动容器内；改为固定面板中的独立方形画布，仅工具区滚动，绘制组件显式裁剪。截图现象与这些风险一致，真机修复效果仍待验证。
- 已添加边框被后面的普通素材样式覆盖；提高已添加状态选择器优先级，选中状态继续用底色/阴影区分。重复添加保留原有逻辑。

## 待确认行为

1. 橡皮擦改为局部擦除：沿手势连续擦掉碰到的线段，未碰到的部分保留，不用背景白色覆盖。一次擦除可以撤销。实现必须兼顾荧光透明度、分段数量上限与云端成图，不只替换文案。
2. 粗细是笔宽，不是文字字号。横向滑动提供4、8、12、16、24、28、40、56、72、96档；旧草稿仍可恢复。新增档位必须同步前后端校验和贴纸缩放规则；先更新现有renderer兼容再启用客户端，不能让线上011拒绝新笔宽。
3. 增加明确的“写画 / 移动画布”模式，避免一根手指既滚动画面又落笔。建议固定2160×2160工作区、1080×1080窗口；移动模式拖动时纸面跟手，反向移动可查看另一侧，边界停止；写画模式继续写。生成贴纸时保留全部工作区内容并等比缩到现有1080协议，而不是只保存当前可见窗口。保存草稿记住工作区和视口；旧1080草稿保持原比例。不是无限画布，不每次移动请求云端。
4. 原“往后/往前”目前是逐层移动，改为“置底 / 置顶”，一次移动到程序化装饰最底/最顶；手写贴纸仍是独立上层。若要求手写贴纸与程序化装饰也互相覆盖，需要另行统一层级模型，本次不暗改。

## 实施边界

确认后再写逐文件实施计划，覆盖分段擦除、撤销/重做、视口坐标、完整导出、旧草稿兼容、笔宽前后端一致和层级测试。沿用当前字体/审核链路；不创建资源，不更改网络/CDN，不推送或合并。真机验证不能用结构测试替代。
