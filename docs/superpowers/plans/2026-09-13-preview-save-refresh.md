# 微信预览保存实施计划

用户最新纠正：入口从单张改整组。执行顺序：修改回归断言观察旧逻辑失败；移除当前图筛选，使用manifest顺序与真实进度/续存；移除提示并缩短底栏；完整检查、截图与新预览码，记录未部署边界。

用户已确认设计。仅当前工作树实现，不部署、不切流。

1. 修正编辑排序按钮尺寸和区域高度；预览导航、动画开关、猫咪头像及间距。
2. 两个趣味字画入口传递项目快照；预览页按sceneId锁定当前目标，使用最终审核返回图并编号后保存，禁止未审核URL进入保存。
3. 黑白主题进度浮层、重复点击锁、失败和退出保护，复用同指纹审核结果。
4. 新增审核成功/失败、缓存、选择及静态外观回归；运行全部检查，记录未验证项并生成开发预览。
