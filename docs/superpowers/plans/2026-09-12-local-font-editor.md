# 本机编辑实施计划

1. 增加可复现字体资源分包构建、第三方解析库来源及授权，校验包体和重组哈希。
2. 新增字体加载/绘制模块、显式回滚开关，scenePainter接受轮廓文本绘制依赖；live-preview接入本机底图与最新场景保护。
3. 输入页本机规则生成；预热仅加载字体；候选“用这套”进入编辑，禁止本机模式错误降级请求云端。
4. 补无云调用、三字体中文、缺字、异步去重/重试、过期场景回归；开发者工具轻量冒烟。保留审核导出，不改线上服务。
5. 运行治理完整检查，更新current、decisions及当日记录，说明本机/云端边界和未验证项目。
