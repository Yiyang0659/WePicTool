# 实施计划

依据已确认规格 2026-09-13-hunyuan-outfit.md。

1. 新增纯适配器校验参考图片、调用官方 generateImage、校验输出；无重试或供应商回退。
2. processOutfit 替换成图出口，SDK升级4.0.2，保留既有转存与分类；缺Key不mock。
3. 回归真实入口的适配器请求、失败不重试、非法输入、缺SDK及返回值；运行完整治理检查。
4. 更新状态、运行文档及提交步骤。用户在现有云函数上传并部署云端依赖；保留所有环境变量，仅核对运行时和超时兼容。真实效果及抵扣在云端另行验收，禁止本地工具消耗成长计划额度。
