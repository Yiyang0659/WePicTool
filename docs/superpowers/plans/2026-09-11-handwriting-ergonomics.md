# 手写交互实施计划

依据：已确认的2026-09-11-handwriting-ergonomics设计。

1. 扩展共享funStrokes笔宽白名单；增加局部线段裁剪橡皮擦，保留旧整笔函数兼容，跨触摸点连续采样；数量超限原子拒绝并提示，不丢草稿。
2. handwritingLibrary保存2160工作区和视口，旧草稿默认1080；inkStickers完整工作区归一化再变换。只将1080受限笔迹发云端，不发送视口。
3. fun-live-preview本地视口坐标映射与独立平移模式；平移不产生笔迹或网络调用，取消恢复视口；笔迹历史独立。
4. 编辑页接通模式/视口/保存/恢复/横向笔宽；程序化装饰一键移至首尾，文字相对层级保持。
5. 添加算法、组件、草稿与导出回归，执行全套治理检查。新宽度未确认云端兼容前不宣称可上线；原服务部署只允许既有保留网络脚本，遇阻报告。
6. 更新状态、迭代记录、README；真实触摸尚未验证必须明确标记。不推送或合并。
