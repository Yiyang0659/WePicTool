# 实施计划

1. dressup入口新建空组，旧草稿显式恢复；其他组仅有AI归入素材后展示，AI回填保留本项目用户素材。
2. funTextProject新增保留场景身份/内容的作用域风格操作，样式控件引用当前场景风格；前后端允许各场景独立风格。
3. 样式范围上移、重命名风格；去除纯背景空卡禁令，保留未部署协议能力保护，不伪造线上支持。
4. 覆盖7张新增/重排/风格/预览及保存协议测试，全量检查并记录。云端部署未授权切流，不修改线上能力声明；交接现有renderer更新需求。
