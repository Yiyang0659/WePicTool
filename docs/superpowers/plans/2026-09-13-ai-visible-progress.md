# AI真实进度实施计划

依据同名已确认设计。

1. 新建可测试逐图队列：并发2、稳定索引、严格结果检查、取消隔离、失败分类、成功结果聚合。
2. outfit-import 接入队列；增加真实计数、耗时、状态缩略图；部分失败保留会话结果，显式重试失败项/仅导入成功项；上传失败也不可静默丢失。
3. 不修改云函数协议或模型调用，不新增付费调用、数据库、部署或自动重试。保留每图审核。
4. 补充乱序、并发、失败、取消、重试、错误提示测试，运行完整治理检查；记录真机和部署未验证边界。
