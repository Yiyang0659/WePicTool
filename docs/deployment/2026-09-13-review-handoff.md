# 2026-09-13 代码审查与部署交接

## 结论

后续本地修正：客户端已取消能力预查询，直接走render-stack审核保存；下述能力门禁是016部署时的历史实现。七张卡滑动已修正为完整列表循环。用户接手功能测试，本次未继续部署。

本轮修复尚未合并或全量切流。2026-09-13 22:34 renderer候选016已构建normal，开发版`gray015=1`定向016、012保持默认；真实小程序私有链路仍待真机验收，processOutfit仍未部署。生产仍不可直接提交；业务代码审查不等于微信平台审核。审查覆盖近期AI、卡片编辑/保存、打包配置，非全仓安全认证。

## 已处理发现

1. 空白卡/混合风格被固定false开关永久阻断：现在在保存前经同一callContainer服务与灰度参数查询POST `/render-capabilities`。新版返回严格的protocol=1/freeCards=true后才发起保存；旧服务/不支持返回明确提示，鉴权/网络错误保留原诊断。普通模板无额外请求，不缓存能力，不绕过最终审核。云端必须部署新版端点与兼容renderer后才实际恢复。
2. processOutfit审核暂不可用也删除原图，重试引用失效fileId：仅明确CONTENT_UNSAFE删除，异常仍阻断AI但保留原图以重试。
3. 纯色自由卡修改配色/背景使文字不可见：与首次加字一致，从当前配色选择亮度差最大的文字色，回到模板恢复该模板配色；不改手写与装饰，撤销保留。
4. 穿搭旧备份写入但不读取：重进后恢复最新草稿仍可切换到持久备份，恢复动作同步保存两槽。
5. 实验故事规划缺少审核SDK却返回通过：现改为SAFETY_UNAVAILABLE。该实验函数依赖仍越出独立部署目录，不纳入当前可部署交付物；启用前另补独立包依赖与验收。
6. 小程序包统计排除cloudhosting而配置未明确排除：两入口配置显式排除对应服务源码目录；实际微信上传清单仍待核验。
7. 底栏图片静态预检缺失：增加六条路径、PNG数据/尺寸与解压检查。已有图标完整CRC/解压检查通过；截图破图未现场复现，不宣称根因是缓存或已修好。

## 部署顺序与权限边界

本轮最终验证：`npm test` 452/452、95个首方JS语法、小程序普通预检、TypeScript、带call-container-only声明的release预检通过。预检子进程曾在强制process.exit的V8线程退出中阻塞，改用exitCode后完整重跑通过。文档检查和diff空白检查亦通过；这些不包含云端或真机验收。

- 使用当前工作树，不上传桌面旧副本。先冻结/提交审阅后的候选，再安排合并，保留用户其他未提交改动。
- 保存服务源码包：`/tmp/wepic-release-candidate-WZlkRt/fun-card-renderer.zip`，SHA256 `2565e0ed297f1f6eb7cc11a8b9b4c3ded32fe77a02ddfcaf69bd29b9c78ff62e`。仅服务JS、Dockerfile、依赖锁、字体与许可，不含本机.env和node_modules；这是待云构建源码包，不是已验证Linux镜像。
- 先只读核对现有版本/路由，再在原fun-card-renderer服务发布兼容候选，保留稳定版本。不要直接执行旧部署脚本的--execute：它默认FULL且改变审核变量。
- 若做定向灰度，`/render-capabilities`与`/render-stack`必须命中同一候选版本；当前客户端仍gray015，仅开发版使用，未擅自改云路由。体验/正式包走默认路由，必须同步验证默认服务兼容后才能放量。
- 新版验证后无需盲开SUPPORTS_FREE_CARDS；保持false可以每次查询服务器。回滚旧服务将明确阻断新卡，不伪造保存成功。
- 再部署processOutfit（云端安装依赖），然后重新编译小程序；水印与审核重试修复均在该函数。
- 验收7张混合卡重排、当前页/整组保存、文字/图片审核拒绝、弱网重试、相册权限、iOS/Android、云存储权限和过期文件。

## 仍需处理/用户提供

- 底栏破图：提供当前开发者工具导入路径、资源请求报错或实际新包复测；包内文件通过不能证明运行包一致。
- 云端候选构建、访问控制、真实审核与相册保存、流量切换；本机未发现Docker命令，未做Linux构建。
- 主体/备案/服务类目、算法材料、隐私指引、域名、预算及免费额度到期策略。
- 实验planFunTextStory不是当前可独立部署范围；如首发要启用需补依赖闭包。不能把本次修复审核失败分支当作实验能力已验收。
- 真实生产路由及网络权限仍是上线门槛；静态release命令通过不代表这些门槛通过。
