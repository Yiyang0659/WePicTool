# 2026-09-04 迭代记录

## 本次目标

为穿搭叠图、分层云换装和趣味字画设计统一的导出与顺序识别契约；本次只形成待确认设计，不编写实施计划，不修改业务代码。

## 完成内容

- 新建 `codex/unified-stack-export` 功能分支，基于当前 P1/P2.1/P2.2 共同基线继续工作。
- 新增 `docs/superpowers/specs/2026-09-04-unified-stack-export-design.md`，定义统一 manifest、图片内可见序号、串行保存、失败续存、三玩法适配、兼容降级和真机验收矩阵。
- 核对微信保存接口契约：`wx.saveImageToPhotosAlbum` 只有本地 `filePath`，没有目标文件名或系统相册排序参数。因此设计不再承诺通过 `01/02` 文件名控制相册或微信顺序。
- 固定本次范围不包含回流卡、埋点、穿搭编辑、素材包或趣味字画视觉改版，避免一次任务承担多个目标。
- 同步 `docs/current.md` 和设计索引，明确当前等待用户确认，尚无业务行为变化。
- 修正 `docs/product/TECHNICAL_SPEC.md` 的现状误记：共享 exporter 只有顺序保存和续存，没有最终图片编号或相册文件名控制；回流卡与统一埋点均尚未实现。
- 用户确认统一导出设计后，新增 `docs/superpowers/plans/2026-09-04-unified-stack-export.md`，拆为 8 个按测试驱动执行的任务：manifest、角标合成、manifest 保存、穿搭接入、分层云换装接入、趣味字画接入、预览/引导迁移和最终验收。
- 同步设计状态、当前状态和设计/计划索引；当前实施计划待用户确认，业务代码仍未修改。

## 验证结果

2026-09-04 在 `/Users/Zhuanz/Desktop/WePicTool/.worktrees/fun-text-stack-phase1` 执行：

```text
npm run check:docs            通过，文档治理检查退出 0
git diff --check              通过，无空白错误
```

实施计划结构自检通过：共 8 个任务，Task 1–7 各包含连续 Step 1–5，Task 8 包含 Step 1–6；未发现 `TBD`、`TODO`、`FIXME` 或占位文件路径。

未运行微信开发者工具、iOS、Android 或真实微信聊天；本次没有业务代码变化。

## 遗留风险

- 系统相册和微信图片选择器的实际排列行为依赖平台与设备，只能在双端真机记录，设计不作程序性保证。
- 序号角标需要本地 Canvas 再合成，仍需通过实施阶段验证大图内存、透明 PNG、画质与耗时。
- 当前分支的 Docker、CloudBase 云托管和趣味字画线上链路仍为 NOT READY，本次设计不解决这些部署阻塞项。

## 下一步

1. 用户确认统一导出实施计划。
2. 确认后从 Task 1 开始补失败测试和最小实现。
3. 完成每项聚焦回归，最终执行完整自动化与真机验收矩阵。

README 无需更新：本次只有待确认的内部设计说明，没有改变可运行功能、项目入口、运行方式或部署方式。

## 统一叠图导出 Task 1

完成统一导出 manifest 纯数据底座：新增三玩法适配器、两位序号、独立叠顺序、唯一封面、严格 URL/身份/连续序号校验、稳定指纹和按 manifest 顺序展开能力。`others` 不进入穿搭叠图；每个分层换装组独立编号；趣味字画严格按选中场景匹配渲染卡片。

TDD 记录：

```text
RED: node --test tests/stack-export-manifest.test.cjs
     0/7 通过，模块不存在，符合预期
GREEN: node --test tests/stack-export-manifest.test.cjs
       7/7 通过
回归: 40/40 通过（manifest、layered-dressup、fun-text-project）
npm run check:syntax
       通过，递归检查 47 个首方文件
git diff --check
       通过
```

README 无需更新：Task 1 只新增尚未接入页面的内部导出数据契约，不改变用户可访问行为、运行方式或部署方式。

## 统一叠图导出 Task 2

完成序号角标 Canvas 物化底座：角标按图片短边计算并限制安全尺寸，固定右上半透明深色胶囊和白色两位数字；支持 1:1、4:5、3:4、横竖图，保留 PNG 或高质量 JPEG 输出，严格串行处理并通过 generation token 拒绝旧异步结果。图片解析、解码或导出失败会返回具体叠与序号，不继续处理后续卡片。

TDD 记录：

```text
RED: node --test tests/sequence-badge-composer.test.cjs
     0/6 通过，模块不存在，符合预期
GREEN: node --test tests/sequence-badge-composer.test.cjs
       6/6 通过
回归: 19/19 通过（角标、image-exporter、fun-card-canvas）
npm run check:syntax
       通过，递归检查 48 个首方文件
git diff --check
       通过
```

README 无需更新：Task 2 新增内部图片物化模块但尚未接入用户页面，不改变当前入口、运行方式或部署方式。

## 统一叠图导出 Task 3

完成 manifest 保存与断点续存语义：新增 `saveExportManifest`，保存前严格校验 manifest、指纹和全部本地 `exportUrl`，只按 manifest 顺序串行写入相册，不在保存阶段下载或重绘。进度与失败信息包含叠身份、两位序号、全局游标和 manifest 指纹；失败后可从 `nextIndex` 继续，旧 URL 数组接口保持兼容。

TDD 记录：

```text
RED: node --test tests/image-exporter.test.cjs
     新增 3 个 manifest 保存用例失败，入口尚不存在，符合预期
GREEN: node --test tests/image-exporter.test.cjs tests/sequence-badge-composer.test.cjs
       12/12 通过
回归: 48/48 通过（image-exporter、角标、趣味字画结果、分层换装）
npm run check:syntax
       通过，递归检查 48 个首方文件
git diff --check
       通过
```

测试加载器同步显式注入 `stackExportManifest` 依赖，避免 CommonJS 沙箱无法解析相对模块时误走空 exporter 降级。

README 无需更新：Task 3 只扩展共享导出能力，尚未改变用户页面入口、运行方式或部署方式。

## 真机调试主包超限修复

自动真机调试返回 `80051 source size 2265KB exceed max limit 2MB`。核对开发者工具实际打开的 `codex/unified-stack-export` 工作树后，确认不是 AppID、网络或项目入口问题，而是小程序主包真实超限：未计云函数和云托管目录的源码候选为 2,456,489 bytes（2.343 MiB），其中三张 640×640、不含透明通道的头像 PNG 合计 1,604,105 bytes。

完成内容：

- 将 `head1`、`head2`、`head3` 从 PNG 转为质量 82 的 640×640 JPEG，合计由 1,604,105 bytes 降至 190,071 bytes；旧 PNG 已从 Git 工作树移除，可通过版本历史恢复。
- 同步更新素材包封面、三张头像素材和首页演示引用；素材内容、尺寸和用户流程不变。
- 素材完整性测试改为校验 JPEG 文件头、实际 640×640 尺寸及单张小于 150 KiB。
- 上线预检新增主包源码保守估算：排除独立的 `cloudfunctions`、`cloudhosting`、`node_modules` 和已声明分包；超过 2 MiB 直接失败，超过 1.8 MiB 给出预警，防止问题回归。
- 修复后主包源码候选为 1,044,089 bytes（0.996 MiB）。

TDD 与验证记录：

```text
RED: JPEG 素材测试失败，注册表仍引用 head1.png；主包体积测试失败，预检未拦截 2 MiB fixture。
GREEN: 两项聚焦测试均通过；npm run check:miniprogram 通过。
npm test: 未通过；本次执行期间新增的未跟踪 tests/outfit-stack-export.test.cjs 有 5 项统一导出在途测试失败，失败点为尚未实现的 result 页面接口/Canvas，与本次素材和包体修复无关；其余测试通过。
聚焦回归: node --test tests/layered-dressup.test.cjs tests/check-miniprogram.test.cjs，31/31 通过。
npm run check:syntax: 通过，递归检查 48 个首方文件。
npm run check:miniprogram: 通过；仅保留既有 renderer 生产配置提醒。
npm run lint: 通过。
npm run check:docs: 通过。
git diff --check: 通过。
```

仍待验证：在微信开发者工具重新点击真机调试，确认服务端上传成功，并分别完成 iOS/Android 行为验收。

README 无需更新：本次只修复内置素材编码和新增构建期包体防线，不改变项目入口、运行方式、部署方式或用户流程。

## 统一叠图导出 Task 4

完成穿搭结果页统一顺序图接入：白底卡合成完成后，使用独立 `sequenceBadgeCanvas` 串行生成图片内可见序号；上衣、下装、鞋子分别从 `01` 开始，其他素材不进入叠图。缩略图、微信预览、单图保存、整组保存和保存全部复用同一 `exportUrl`，物化后不再叠加 CSS 序号。

页面新增导出准备、manifest 指纹、保存游标和错误状态；切比例、切原图/白底、改分类和重做会清空旧顺序图与续存会话。整组保存继续执行 3 张门槛，保存中断会提示具体组名与编号，并从失败项续存；准备与保存均隔离重复点击及过期 generation。

TDD 与验证记录：

```text
RED: node --test tests/outfit-stack-export.test.cjs tests/task.test.cjs
     新增 5 项穿搭导出用例失败，页面入口和独立 Canvas 尚不存在，符合预期
GREEN/回归: 24/24 通过（穿搭导出、task、预览布局、image-exporter）
npm run check:miniprogram
       通过；仅保留既有 renderer 生产配置提醒
npm run check:syntax
       通过，递归检查 48 个首方文件
git diff --check
       通过
```

README 无需更新：本任务改变的是既有穿搭结果页的导出实现与提示状态，不改变项目入口、开发命令或部署方式。

## 统一叠图导出 Task 5

完成分层云换装四叠统一导出：头像/发型、上衣、下装、鞋子按注册表顺序各自从 `01` 编号；内置素材、用户素材和混合素材统一经独立 `sequenceBadgeCanvas` 物化。页面缩略图、单组预览、微信预览、保存一组和保存全部均消费同一 manifest 的 `exportUrl`。

新增 fingerprint、generation、保存游标和重复点击隔离。添加、删除或移动素材会废弃旧导出会话并重新生成；不足 3 张的组仍可预览带编号图片，但不能作为微信叠图保存。保存中断会显示部位组与编号，重试从失败项继续；页面卸载后旧物化结果不能写回。

为避免与同工作区正在进行的头像素材压缩测试改动混在一起，本任务新增独立 `tests/dressup-stack-export.test.cjs`；仅在既有测试加载器位置补充两个新模块依赖。

TDD 与验证记录：

```text
RED: node --test tests/dressup-stack-export.test.cjs
     0/5 通过，页面接口和独立 Canvas 尚不存在，符合预期
GREEN/回归: 25/25 通过（分层换装导出、既有 layered-dressup）
npm run check:miniprogram
       通过；仅保留既有 renderer 生产配置提醒
npm run check:syntax
       通过，递归检查 48 个首方文件
git diff --check
       通过
```

README 无需更新：本任务只统一既有换装页的预览与保存图片，不改变项目入口、运行方式或部署方式。

## 统一叠图导出 Task 6

完成趣味字画单叠统一导出：只有远端或明确网络错误后的本地 Canvas 渲染成功、且 scene/card 身份与顺序严格匹配时，才使用独立 `funTextSequenceBadgeCanvas` 生成 `01…N` 可见序号。结果轮播、“先滑着看看”和“按顺序保存”复用同一 manifest 的 `exportUrl`；旧异步结果由 render/export 双 generation 隔离。

保存失败时按钮显示具体“从 03 继续保存”，并按 manifest 游标续存。内容安全错误、生产响应错误、入口关闭、版本不支持或卡片身份不匹配均不会进入角标和保存链路。原始渲染 fingerprint、scene JSON 和记录缓存不写入导出角标字段；角标物化失败不新增成功历史，也不会删除已存在的其他合法记录。

TDD 与验证记录：

```text
RED: node --test tests/fun-text-result.test.cjs tests/fun-text-emergency.test.cjs
     新增 4 项趣味字画导出用例失败，页面入口和第二块 Canvas 尚不存在，符合预期
GREEN/回归: 43/43 通过（趣味字画结果、安全开关、记录恢复、image-exporter）
npm run check:miniprogram
       通过；仅保留既有 renderer 生产配置提醒
npm run check:syntax
       通过，递归检查 48 个首方文件
git diff --check
       通过
```

README 无需更新：本任务只替换趣味字画结果页的预览与保存导出层，不改变入口、运行方式或部署方式。

## 统一叠图导出 Task 7

完成微信预览 manifest 契约迁移：新调用方只传 `{ manifest, selectedStackIds, ratio }`，预览页严格只读取 `exportUrl`，任一选中卡片缺少编号图时 fail closed；叠顺序、组内顺序、`01` 标签和封面身份保持不变。旧 `{task}` 与 `{groups, ratio}` 继续兼容一版，并明确标记为 legacy。

预览页现有滑动、展开/收起、稳定图片节点、查看大图和长按操作保持不变；长按保存消费当前 manifest 编号图。穿搭、分层换装、趣味字画三个新调用方不再夹带裸 task/groups。三处发送引导统一为：每次只发送一叠、按图片角标 `01…` 勾选、确认 `01` 在第一位、勾选“发送后合并展示”；不承诺文件名或系统相册自动排序。

TDD 与验证记录：

```text
RED: node --test tests/preview-layout.test.cjs tests/stack-send-guide.test.cjs
     新增 6 项契约/引导用例失败，manifest 输入与统一文案尚不存在，符合预期
GREEN/回归: 69/69 通过（预览、三玩法导出、分层换装既有行为、趣味字画结果）
npm run check:miniprogram
       通过；仅保留既有 renderer 生产配置提醒
npm run check:syntax
       通过，递归检查 48 个首方文件
git diff --check
       通过
```

README 无需更新：本任务升级既有预览输入契约和引导文案，不改变项目入口、开发命令或部署方式。

## 统一叠图导出自动化与治理收口

统一 manifest、图片内可见序号、三玩法接入、预览契约和发送引导的分支编码已完成。保存任务也绑定 export generation：用户在保存未结束时修改穿搭、切换换装项目或开始新的趣味字画项目，旧保存成功/失败均不会写回新页面的引导、断点或错误状态。长期文档已同步：PRD 将 F-34 改为“已实现，待真机验收”；PLAYBOOK 删除文件名控制顺序的错误承诺；TECHNICAL_SPEC 记录 manifest v1、badge style v1、fail-closed 预览和旧输入兼容周期；current/roadmap 保留生产与真机证据边界；decisions 记录可见角标取舍。

完整验证于 2026-09-04 在 `/Users/Zhuanz/Desktop/WePicTool/.worktrees/fun-text-stack-phase1` 执行：

```text
npm test
  251/251 通过，退出 0
npm run check:syntax
  通过，递归检查 48 个首方文件，退出 0
npm run check:miniprogram
  通过，退出 0；保留 renderer access mode、HTTP loopback 和生产 HTTPS 提醒
npm run lint
  TypeScript 无错误，退出 0
npm run check:docs
  文档治理检查通过，退出 0
git diff --check
  通过，无空白错误
```

视觉与外部验证边界：自动化已覆盖 `01`、两位数字、1:1/4:5/3:4/横竖图的安全布局、深色胶囊与白字绘制调用、PNG/JPEG 保真参数和串行物化；本次没有可运行微信 Canvas 2D 的真机环境，因此白衣白底、黑板、复杂照片、透明 PNG 的实际像素输出与主体遮挡仍标记为**未验证**，没有提交模拟截图代替证据。微信开发者工具、375px/常见 Android 屏宽、iOS、Android、系统相册排列、真实微信聊天、授权拒绝和设备端失败续存也均未验证。

README 已更新：统一可见序号导出是用户可感知的新能力，README 已增加三玩法共享导出流程、API 能力边界和三个核心工具文件说明。

`docs/changelog.md` 未更新：功能仍在 `codex/unified-stack-export` 分支，尚未合并或发布。

下一步只剩外部验收：先在微信开发者工具跑三玩法完整矩阵，再做 iOS/Android 和真实聊天；完成前不得把 P0 或生产状态标为 READY。

## 趣味字画真机渲染异常修复

真机选择首页示例“我今天想见你”后，结果页显示“服务端渲染响应异常”。定位到当前环境虽配置了云托管服务名，但 `fun-card-renderer` 仍未完成部署、权限和线上冒烟验证；`wx.cloud.callContainer` 返回非成功响应后被归一化为 `INVALID_RENDER_RESPONSE`，而结果页只允许“未配置”和网络失败进入已有的本地 Canvas，导致开发真机验证被不必要地中断。

修复内容：

- 以现有 `FUN_CARD_RENDERER_URL` 回环地址作为开发模式边界，仅在 `localhost`、`127.x.x.x` 或 `[::1]` 配置下允许 `INVALID_RENDER_RESPONSE` 转入本地 Canvas。
- HTTPS 生产 renderer 的异常响应继续失败关闭；`CONTENT_UNSAFE`、`SAFETY_UNAVAILABLE` 和其他未知错误继续禁止本地绕过。
- 增加回环开发配置成功降级和 HTTPS 生产配置拒绝降级的回归测试。

TDD 与验证记录：

```text
RED: node --test --test-name-pattern="loopback renderer config" tests/fun-text-result.test.cjs
     0/1 通过，INVALID_RENDER_RESPONSE 直接进入失败状态，符合预期
GREEN: node --test tests/fun-text-result.test.cjs
       25/25 通过
npm test: 242/242 通过
npm run check:syntax: 通过，递归检查 48 个首方文件
npm run check:miniprogram: 通过；保留当前 HTTP 回环 renderer 不可发布的预期提醒
npm run lint: 通过
npm run check:docs: 通过
git diff --check: 通过
```

真机仍待验证：重新进入“趣味字画”示例，确认结果页生成全部卡片并可预览、保存。

README 无需更新：本次是开发环境异常降级的 bug 修复，不改变入口、运行命令、部署方式或生产安全策略。
