# 2026-09-08 renderer 超时排查与编辑器取图修复

## 范围与结论

- 当前分支 `codex/ai-outfit-picker-flow`；未合并 main、未推送 GitHub。本轮未修改 VPC、子网、NAT、公网、CDN/CORS 或创建存储资源。
- 先前记录的真实 `/render-stack` 调用在微信侧返回 `102002 请求超时`；当前尚未取得该请求对应的最底层容器运行错误，不能把超时包装视为根因修复。
- 服务增加审核等待上限 6 秒（关闭式返回 `503 SAFETY_UNAVAILABLE`）、最终渲染等待上限 20 秒（`504 RENDER_TIMEOUT`）、HTTP handler 拒绝兜底（`500 INTERNAL_ERROR`），以及最终渲染阶段日志。三款镜像内字体路径未改变。
- 部署命令返回 `CreateVersion: finished`、`ReleaseVersion: finished`、`Deployment successful`。只读详情确认在线版本 `fun-card-renderer-008`、服务状态 `normal`、端口 8080；镜像为 `ccr.ccs.tencentyun.com/tcb-100050496765-lapk/ca-pcsdrzau_fun-card-renderer:fun-card-renderer-008-20260908220501`。平台正常不等于业务链路通过。
- 编辑器原有诊断改动漏掉了 `renderedCandidate` 变量定义以及过期请求检查，收到成功响应也会抛 ReferenceError。恢复按当前 candidateId 取图及过期请求检查，保留原诊断功能；现有失败回归测试转绿。此客户端改动尚未由真机验证。
- 收尾修复：入口在运行配置校验通过后才加载 `wx-server-sdk`，使错误配置不再先等待庞大 SDK 初始化；移除未使用的日志辅助函数。这两项仅在本地，尚未再次部署。

## 验证记录

- renderer 专项测试 27/27 通过，包含新增的审核超时、最终渲染超时和 HTTP 异常 JSON 响应测试。
- 初次 `npm test` 为 309/312：两个入口测试的 1500ms 子进程期限在并发负载下失败，单独重跑 4/4 通过；一个编辑器取图测试稳定失败，已找到未定义变量并修复，定向重跑 1/1 通过。
- 编辑器修复后的串行全量为 310/312，两个入口期限测试再次失败，说明不能简单归因于测试并发。源码确认入口先加载 SDK、后校验环境，已调整加载顺序而未放宽测试期限。
- 收尾语法（54 个首方 JS）、TypeScript、普通及发布预检、文档检查与 `git diff --check` 通过。
- 最后一次 `npm test` 仍为 310/312，失败项仍是两个启动子进程的 1500ms 期限；直接启动缺失环境配置的进程，在 5627ms 后按预期返回状态 1 与 `CLOUDBASE_ENV_ID is required`。当时只读进程采样看到两个 ego Renderer 分别约 449.6% 和 416.4% CPU；高负载可能影响计时，但不是云端问题证据。未更改用户浏览器进程，未放宽测试断言，未宣称全量通过。
- 部署后 automator 曾返回 `timeout waiting for automator response`；重建自动化入口明确得到微信开发者工具原始错误 `需要重新登录（code 10）`。端口 51181/9440 曾处于监听状态，开端口不足以证明登录有效。
- 没有取得部署后真实 callContainer 的成功图片、请求状态码、requestId 或对应业务日志，因此中文字体、内容安全、云存储上传以及 renderer 运行时无 CDN 请求均仍标记未验证。客户端字体 CDN 请求与容器字体读取不能混用为证据。

## 剩余事项

1. 用户重新登录微信开发者工具后执行一次轻量真实调用，定位审核/绘制/上传的具体失败阶段；当前不继续盲目部署。
2. 当前超时包装不会取消底层渲染/上传；preview 渲染阶段没有总超时；诊断日志还没有请求级关联。这些限制需在后续最小修复中处理，不宣称当前已完成所有链路修复。
3. 服务端主预览路径切换与三字体实际图片验证尚未完成，小程序保持 NOT READY。

README 无需更新：本次为现有 renderer 错误兜底和编辑器取图 bug 修复，入口与部署命令未改变；实际部署状态和限制记录在 current、部署手册和本日志。
