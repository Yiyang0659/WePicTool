# 2026-09-10 授权后真实审核复核

## 编辑页参考图布局设计

- 用户要求按图还原界面、按钮和功能分布。只读核对当前WXML，识别独立多草稿/手绘转贴纸为新增行为，而非单纯视觉修正。
- 已写待确认设计editor-reference-layout.md：保留上方大画布、还原下方操作区；真实本机手写库与矢量贴纸不绕过最终图片审核。不添加假的历史缩略图，不模拟系统栏。
- README 无需更新：本轮仅待确认设计，页面、功能与部署均未更改。文档治理与git diff --check通过；未重跑功能测试，不声明新界面已实现。

## 本地交互与自由手写规划

- 启用后重新检查349/349测试、60文件语法、普通预检、TypeScript、文档治理与diff检查通过；未把静态通过当成新界面实测。

- 23:07用户报告登录后：当前分支正确；开关原为false是看不到新功能的确定原因。真实单卡预览200/1108ms，含一笔手绘的五张最终图200/2746ms，项目funtext_1789052852108。首张下载到/tmp/wepic-011-ink-smoke.png并目检：中文、爱心、蓝色笔迹完整。正常图片审核与上传链路通过；没有逐字库或风险图片实测。
- 已启用ENABLE_FUN_LOCAL_EDITOR，并解除启用状态下的客户端手绘导出临时阻断，服务端审核仍强制；回滚状态继续阻断笔迹导出。更新回归覆盖两种开关状态。
- 开发者工具9442登录API成功，但自动导航报pageNotFound异常，CLI指定正确目录重新加载返回code10需登录。两者不混为同一登录状态；已请用户手动编译。新界面/真实触摸未验证，不宣称设备上已生效。README 已更新启用与验收边界；无网络/资源/Git推送变更。

- 最终本地检查：349/349测试、60首方JS语法、普通小程序预检、TypeScript、文档治理、声明式发布预检及git diff --check全部通过。声明式预检不代表公网/真机门禁通过；未改公网。云端011新功能及真实UI仍未验证，交付保持进行中。

- 22:47补充：011正常、100%、8080，镜像fun-card-renderer-011-20260910222927。wx.login返回“需要重新登录,access_token missing”，真实新接口请求未到容器；已直接告知用户重新扫码，未继续盲目部署。客户端开关及手绘导出门禁保持关闭。
- 补失败测试后实现已完成笔迹独立Canvas缓存（移动帧仅重画活动笔迹）、切卡/退出手写取消未完成笔迹、1000点上限提示，定向3/3通过。真机帧率、触控和云端笔迹审核成图尚未验证，不宣称完整交付。
- README 已更新：说明本地交互实验、开关关闭和登录阻塞；以下较早规划/348测试记录保留为过程证据，最终检查结果见后续记录。

- 用户确认书面设计后开始实施。新增单卡预览、底图缓存、Canvas本地装饰/笔迹，以及候选大框点击与当前卡交接；基础笔迹校验/整笔擦除/撤销/风格切换保留笔迹/最近一个本机草稿。新增行为先跑失败测试再实现。
- 完整检查348/348，60首方JS语法、TypeScript、声明式发布预检通过；普通预检和文档治理亦通过。新开关保持false，真实UI与手感未验收，笔迹覆盖层每帧仍会重画已完成笔迹，性能缓存优化尚未完成。
- 本机以真实现有AppID/AppSecret调用微信stable_token(force_refresh=false)及img_sec_check，对360方形白底黑线图返回HTTP200/errcode0；无密钥/token/用户正文输出。穿搭已有SDK图片审核，但renderer需单独接入；已增加HTTPS图片审核复用token，拒绝/未知/超时失败关闭、上传前检查。此证据不是云端审核通过。
- 原服务源码更新提交成功，requestId=dcaf4e8d-f5ba-4ae1-9ab1-bb546a0e73c8；源码zip在临时wepic-renderer-deploy-pBqJC8，网络对比unchanged=true。不含秘密。构建、新接口和笔迹成图待核实。

- 用户确认总体方向并要求规划实现。补充书面设计，明确预览底图/装饰/笔迹分层、每笔不请求云端、缓存与过期响应隔离、整笔擦除和本地草稿。
- 核对当前 renderer 只有文本审核；新手绘导出必须补图片审核，未配置/超时/拒绝不得输出，不能用测试桩冒充真实通过。
- 本期不包含跨设备草稿同步、照片导入和压感保证；仅设计文档，功能代码与在线010未改，等待书面设计复核。
- README 无需更新：本轮只补编码前设计，没有实现或发布新能力。未重跑功能测试；`npm run check:docs` 与 `git diff --check` 通过。

## 用户填写AppSecret后的真实微信验证

- 从Git忽略的.env.renderer.local仅在内存解析凭据，AppID匹配；stable_token普通模式HTTP200，返回令牌，有效期7200秒。未强制刷新，无40164。
- 连接现有开发者工具9442，wx.login获取code，后端jscode2session换取真实OpenID；调用新wechatSafety模块审核固定文本“今天真开心”，返回ok=true/code=OK。
- 日志只输出凭据存在/匹配、是否得到身份、错误码及审核状态，不打印任何密钥、code、openid、session_key或token；临时App状态已删除，自动化仅disconnect未关闭开发者工具。
- 本机实际审核成功不等于容器网络或云端成图成功；009未部署更新，入口头部可信性门禁仍待解决。无网络、云端配置、Git合并或推送。
- README 无需更新：仅补充已实现审核模块的真实验证结果；本轮运行文档治理与diff检查，不重复全量测试。

## 已确认方案的分支实施

- 新增wechatSafety.js和实施计划；显式wechat-https模式使用固定微信HTTPS端点、stable_token普通模式、内存缓存和并发合并、一次受控鉴权重试、AbortController超时。仅2.0 result.suggest=pass放行，其他结果失败关闭；日志只输出预定义错误码。
- server.js给preview/final审核传独立OpenID上下文，不使用body身份。头部存在性仍不等于公网鉴权，启用前需核验可信入口；未将此实现宣称生产就绪。
- 创建Git忽略的根目录.env.renderer.local（权限600，无秘密值），不会自动加载或部署；新增renderer .dockerignore避免.env和本地依赖/部署配置进入镜像。
- 第一轮定向40/40测试通过；55首方JS语法、普通预检和TypeScript通过。补充双handler上下文回归后的结果见本节后续记录。按用户轻量要求未跑全量测试，无云部署或网络调整。
- 最终定向41/41通过，发布预检、文档治理和git diff --check通过；真实微信审核/成图仍未验证，AppSecret尚未填写，Docker镜像未构建/发布。
- README 已更新：说明分支可选审核模式和秘密准备入口；未暂存、提交或推送任何文件。

## 原环境审核修复方案

- 用户截图证实微信云托管prod环境与原cloud1环境不同且服务为空，不继续该环境部署。
- 只读原服务detail仅输出normal及相关配置键名列表（空），没有输出秘密值；AppSecret配置未发现。
- 读取微信官方最新stable_token和msgSecCheck文档，核对2.0参数、pass/risky/review语义，以及40164 IP白名单和管理员确认错误风险。
- 新建待确认设计说明2026-09-10-renderer-wechat-auth-repair.md。没有改业务代码、部署、密钥、网络或权限；不把方案视为修复成果。
- README 无需更新：本轮仅诊断及待确认设计，运行/部署方式尚未改变。按用户要求不重复全量测试；文档治理和diff检查通过。

## 00:52 现有容器微信令牌取证

- 从现有服务实例列表进入sh Webshell，目标fun-card-renderer-009-68cc9db8c8-swwpj；没有创建开发环境或部署新版本。
- 00:51:31现有服务日志WEPIC_TOKEN_DIAG确认CBR_ENV_ID存在、WX_API_TOKEN/WX_CLOUDBASE_ACCESSTOKEN缺失、令牌文件/.tencentcloudbase/wx/cloudbase_access_token读取返回ENOENT。
- 00:52:13读取/proc/1/environ后仅输出相同三个布尔值，主进程结果一致。没有打印环境全集、密钥、令牌正文或用户正文。终端普通输出无法自动读取，因此只将这两条最小诊断写入/proc/1/fd/1供既有日志查询。
- 对照已安装SDK的src/utils/wxCloudToken.ts：环境令牌缺失时读取上述挂载文件，无文件则返回空令牌。index.js:28的msgSecCheck依赖此路径；与既有INVALID_WX_ACCESS_TOKEN错误一致。直接缺口已确认，平台为何不下发仍未确认，不断言平台不支持，也不以微搭认证代替云调用配置证明。
- 后续优先核对当前CloudBase Run的微信令牌下发能力/授权配置；若该服务不支持，再提出显式微信服务端鉴权的最小改动方案并确认，不绕过内容审核，不硬编码短期access_token。
- 本轮仅运行文档治理与diff检查；按用户轻量验证要求不重复全量测试。README 无需更新：仅诊断及状态记录，无业务或部署方式变更。

- 微信开发者工具重新登录成功，自动化端口9442可连接；浏览器Noel配置已登录正确腾讯云账号。
- 控制台小程序认证页确认“滑一叠”、AppID wxf16280c0ba5c507d、授权成功，更新时间2026-09-09 23:26:46。该页面是微搭授权认证流程，不能替代容器OpenAPI调用凭据的证明。
- 服务页确认008正常、100%流量、1实例；未变更任何配置。
- 00:04:59.064（北京时间）真实 wx.cloud.callContainer POST /render-stack，754ms返回503 SAFETY_UNAVAILABLE；00:05:01容器日志仍为 msgSecCheck:fail -501001 resource system error. invalid wx openapi access_token。未取得响应requestId，未成图或上传。
- 调用位置 index.js:28；SDK走 callCompatibleWxOpenApi 兼容云调用路径。授权成功后仍失败，优先怀疑容器调用与云函数票据上下文不匹配；需进一步确认当前CloudBase容器支持的微信OpenAPI接入方式，不能仅凭微信云托管文档直接替换本环境实现。
- 服务详情页面读取结果包含明文腾讯云密钥；已立即告知用户需轮换，不复述或写入文档。浏览器凭据更改由用户亲自完成，不擅自撤销导致服务中断。
- 未改业务代码、网络、权限、资源、部署或Git。轻量验证仍失败，不声称修复。README 无需更新：本轮仅记录真实验证与阻塞，无功能或入口变化。

## 00:32 用户轮换密钥后的验证

- 只读服务API确认在线009、normal、100%流量、8080、最小实例0；不输出配置秘密值。管理查询requestId为28a11ba9-7954-4688-bcea-32b8304fa5ea，不作为渲染requestId。
- 微信开发者工具auto成功。00:31:48.629真实callContainer POST /render-stack在615ms返回503 SAFETY_UNAVAILABLE；00:31:50的fun-card-renderer-009日志仍为msgSecCheck -501001 invalid wx openapi access_token。
- 用户的更新已产生在线新版本；此结果不能证明新腾讯云密钥所有权限正确，上传尚未执行；旧密钥禁用状态未检查。审核与成图未恢复，继续定位微信OpenAPI调用上下文，不再让用户重复换腾讯云密钥解决不同类型的令牌问题。
- 本轮无配置/代码变更，仅轻量验证。文档治理与diff检查通过，未重新运行全量测试。

## 可信调用修复与部署入口阻塞

- 已逐张下载并查看3张1080PNG：“今天真开心”完整可读，三款字形明显不同，无可见缺字；只能覆盖本次短文本，不证明全部字库。目录下载未产出文件，改为逐文件下载后成功，存于临时wepic-renderer-deploy-Xg50i2/results/{1,2,3}.png。文档治理/diff检查通过；无运行时CDN抓包，客户端主预览与真机仍待验收。

- 部署结果：BuildId2602288365，RunId multi_tenant_1x4cAzxLJOI5Oj；010镜像后缀20260910183843，100%、normal、8080。构建日志含字体非空校验且成功。真实wx.cloud.callContainer /render-stack返回200/ok，marker/playful/headline各一张中文PNG，云存储返回3个fileID（项目funtext_cloudcheck_1789036977548）。未抓包，不把源码无CDN读取路径当成运行时无CDN请求证明；本次非全量测试。

- 续接：新增scripts/deploy-renderer-preserving-network.cjs，通过CLI通用API仅更新原服务源码及合并EnvParam。密钥只经子进程stdin在内存传递；包不含.env/node_modules。首次因AccessTypes数组顺序漂移中止，确认集合相同后排序比较并额外核对旧变量，第二次提交受理requestId=33b2d316-5fee-4d7d-9cc5-a69bf9aa3d17。网络与规格未变，构建/成图待核实。

- 用户确认开始后新增wechatCaller.js、共享客户端wx.login及HTTPS模式强制核验；忽略自报OpenID，失败关闭，敏感凭证不输出/持久化。5秒超时、8并发、每实例120次/分钟核验上限。
- 真实开发者工具获取一次性code，经新模块code2session核验成功，固定测试中文随后经新审核模块返回OK。仅本机验证，不代表009已修复或云端上传通过。
- 定向91/91通过；56文件语法、普通预检、TypeScript通过。首次未设置ACCESS_MODE的发布预检失败；按既有手册显式声明call-container-only后静态发布预检通过，此声明不证明公网关闭，现有生产网络门禁仍未通过。
- CUA连接先30秒、再10秒超时。CLI 3.8.1源码更新分支会强制OpenAccessTypes，未执行该部署命令，未修改云端、网络、主分支或GitHub。已告知用户恢复浏览器连接，密钥不必重填。
- README 已更新：说明客户端一次性登录和云端未验证边界。

## 用户授权后的腾讯云密钥专项实测

- 通过现有CLI查询服务009配置，仅在进程内存解析配套凭据，不写密钥文件。确认SecretId/SecretKey存在、环境ID匹配；使用相同SDK对现有云存储上传一个funtext/diagnostics/keycheck-时间戳.txt诊断文件并删除，上传成功、删除返回SUCCESS。无新存储资源，无用户素材变更。
- 这证明腾讯云凭据本身可完成该环境存储上传/删除，但不等于容器出站或微信审核成功。
- SDK源码进一步确认：src/utils/wxCloudToken.ts会在WX_API_TOKEN缺失时回退到WX_CLOUDBASE_ACCESSTOKEN或/.tencentcloudbase/wx/cloudbase_access_token；文件读取依赖CBR_ENV_ID判定。不能再把SDK云函数兼容路径直接定为错误实现，实际挂载令牌是否存在仍未验证。
- 控制台云托管环境设置仅看到固定出口IP和镜像仓库设置，未操作；出现TCR授权提示也未同意。随后浏览器导航被剪贴板读取超时中断，未执行云端修改。
- README 无需更新：本轮仅凭据和SDK诊断，没有功能或部署方式改变。
