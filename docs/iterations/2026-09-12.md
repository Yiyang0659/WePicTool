# 2026-09-12 趣味字画本机编辑

## 本地测试基线收口
- 用户确认先整理并提交功能分支，不合并 main、不推送、不部署或切流。保留现有审核边界和功能开关，不代表首发范围已确认。
- 本轮重新运行 `npm test` 381/381、`check:syntax`（89文件）、`check:miniprogram`、`lint`、`check:docs` 与 `git diff --check` 全部通过。普通预检仍提醒核验生产私有入口；本轮不进行云端或真机验收。
- 扫描 Git 候选文件中的已知本地密钥值与常见密钥特征，未发现已知秘密复制；一处AKID特征匹配位于生成字体Base64中。此为有限静态扫描，不代替完整安全审计。`.env*` 已忽略；`miniprogram/project.private.config.json` 仅保留本地，不加入提交。
- 已有AI模型/端点调整一并作为工作区快照保留，其真实服务可用性未验证。字体源迁移、生成分包和许可证一并提交，避免仅提交删除项导致字体缺失。
- README 已更新：本次随已有变更提交本地编辑及云端导出说明；同步纠正 current.md 中“每日额度耗尽”的旧阻塞结论。013认证冒烟、AI首发范围与双端验收继续待办。

## 保存浮层原生画布遮挡

- 用户截图显示主画布覆盖进度浮层；普通view已有z-index 10000，但未像改字弹窗一样卸载原生预览Canvas。进度浮层显示期间按wx:if卸载四处原生预览组件，隐藏后恢复；保留离屏导出Canvas，避免破坏成图。
- 回归执行模板挂载条件，覆盖audit/preparing/saving、本地/旧预览及手写状态；修复前失败，修复后 `npm test` 381/381 通过，`check:syntax`、`check:miniprogram`、`lint`、`check:docs` 和 `git diff --check` 通过。上线预检仍提醒正式发布前独立验证 renderer 公网访问关闭。真机视觉仍待用户复核。
- README 无需更新：仅修正已实现弹窗的遮挡，不改变业务或部署。

## 编辑页动态保存反馈

- 用户确认效果图后，编辑页当前页/整组保存和微信预览准备统一使用白色粉紫动态弹窗，替换原生黑色 loading。
- 云端接口没有中途阶段回调，等待文案为“正在审核并生成”，不伪造审核百分比；成功返回后切换准备图片，相册阶段按真实串行回调显示当前张数、已保存数量及进度条。动效仅装饰，不触发轮询或额外审核。
- 保持重复点击锁、审核失败关闭、失败续存及卸载保护。README 无需更新：只调整已有编辑页保存反馈，不改变运行方式或云端审核边界。
- 页面定向回归20/20通过；完整回归380/380、89文件语法、小程序预检、TypeScript、文档治理和diff检查全部通过。iOS/Android真机动画与Canvas遮挡尚待确认。未部署或修改流量。

## 范围

- 用户先要求分析、后确认实现。本轮仅改趣味字画编辑，不改穿搭AI；不部署、不合并、不推送，不修改云网络/CDN/密钥或新建资源。
- 输入与候选使用本机规则，改字/字体/背景/贴纸/笔迹本机绘制；保存、完成制作和微信成品预览仍走原云端审核，不把45009记为修复。逐图审核凭据与永久复用暂未实施。
- 新增三款授权字体11个异步资源分包、OpenType轮廓绘制、ENABLE_FUN_OFFLINE_PREVIEW回滚开关、分包/总包预检。
- 运行时发现wx.loadSubpackage属于小游戏而非普通小程序，改require.async；包内TTF readFileSync报permission denied，改模块携带Base64后本机解码。生成时删除此前本轮生成的冗余font.ttf分片，原字体完整保留，可重新生成。
- 多Canvas普通贴纸在本机底图下不可见，改文字/普通贴纸同Canvas合成；手写层仍独立。变化仅本机重绘，不重新下载字体。
- README 已更新：说明本机范围、首次下载、构建命令、回滚与保存仍审核的边界。

## 45009 复核与诊断日志

- 同一 AppID 的只读额度查询（2026-09-12 09:31，北京时间）返回 `daily_limit=2000000`、`used=2`、`remain=1999998`、`rate_limit.call_count=1050000`、`refresh_second=60`；本轮没有调用 `clearQuota`。
- 只读查询 CloudBase `tcbr` 09:01:00–09:03:59 全量日志共16条，只有1条 `stage=audit-start kind=final`，发生在09:02:36，对应 `fun-card-renderer-012-df5dc4647-vmp75`；同秒只有1条 `WECHAT_AUDIT_45009` 和1条 `stage=audit-end ... status=503`，未发现瞬时重复。
- 静态调用链：`template-result.initProject` 触发一次 `/render-stack`；`onRetryRender` 是显式用户重试；编辑导出由 `_editorExportBusy` 合并同页重复点击；编辑完成按钮新增 `_openingResult` 导航锁；回滚到云端预览时，候选“用这套”也由 `_openingEditor` 导航锁保护。候选/编辑预览失败只在一次性fallback或用户明确重试时请求 `/preview-stack`；`/preview-scene` 仅由非本机预览预热使用。当前 `ENABLE_FUN_OFFLINE_PREVIEW=true`，后台预热路径只执行本地字体warm，不调用renderer。`onLoad/onShow` 没有自动重试审核。
- `wechatSafety.js` 增加安全诊断元数据：`/wxa/msg_sec_check`（或token/image对应API）、HTTP状态、ISO时间、截断原始 `errmsg`、`rid`、正文SHA-256和服务端请求 `traceId`；`index.js` 仅输出白名单字段，禁止token、AppSecret、openid明文和用户正文。服务器为每个请求读取可信追踪头或生成UUID并传入审核上下文。历史日志不含这些字段，需后续部署后才会出现。
- README 无需再次更新：本轮是现有审核故障的只读定位、诊断日志和重复点击防护，不改变已记录的本机编辑/云端最终审核边界；未部署、未合并、未推送。

## 验证

- 自动化：372/372通过；三字体重组与源TTF逐字节/哈希相同，每个资源分包小于2MiB；中文轮廓不同、缺字不默认回退、加载去重/失败重试、过期场景保护、本机输入不调用审核/AI/renderer。
- 语法89个JS通过；小程序发布预检通过；TypeScript通过。预检仍提醒发布前需独立验证服务公网访问关闭，本轮按用户要求未修改网络。
- 微信开发者工具3.16.2真实运行：规则候选正常、本机改字与marker/playful/headline均ready=true且failed=false，截图中文已更新、字体不同。仅对编辑流程临时拦截并计数wx.cloud.callFunction/callContainer，结果0，结束已恢复。每次字体操作后等待350ms再检查正常，不把该采样时间宣称为真实绘制耗时或真机性能保证。
- 本机自动化截图保存在/tmp/wepic-local-marker.png、/tmp/wepic-local-playful.png、/tmp/wepic-local-headline.png（临时诊断文件，不是云端成品）。
- 最后复核/tmp/wepic-local-final-preview.png：候选文字与爱心装饰同时可见，修复了本机底图遮挡普通装饰的问题。
- 最终重新执行372/372测试、89文件语法、发布预检、TypeScript、文档治理及git diff --check，全部通过。
- 本轮追加诊断日志、trace上下文及候选/完成按钮重复点击回归后，重新执行完整检查为379/379测试；89文件语法、小程序上线预检、TypeScript、文档治理和 `git diff --check` 全部通过。

## 剩余风险

## 01:35导出失败复核

- 用户真机截图为保存/完成阶段失败，本机文字已正常显示。通过现有CloudBase CLI会话只读SearchClsLog，查询01:25–01:45；012在01:35:11、01:35:14输出WECHAT_AUDIT_45009。查询RequestId为2e6e6430-d7f8-45f7-a6dc-cc71c6036f6c（查询ID，不冒充成图requestId）。未输出正文、身份或密钥。
- 原始原因链：wechatSafety.js的msg_sec_check非零errcode → server.js auditPayload的503 SAFETY_UNAVAILABLE → funCardRendererClient通用“安全服务暂不可用”。不是字体错误或内容被判违规。
- 官方文档：https://developers.weixin.qq.com/miniprogram/dev/server/API/sec-center/sec-check/api_msgseccheck.html 。45009为每日额度超限；通常4000次/分钟、200万次/天；未上架小程序100次/天。当前AppID是否正式上架未验证，不能把100次限制当作已证实的账号配置。
- 已修客户端：45秒总请求期限与迟到回调保护；SAFETY_UNAVAILABLE后同微信API实例30秒防重复；保存审核失败完整弹窗而非截断toast；结果页加载/失败时不显示“已生成”，失败可以携项目返回编辑。保持失败关闭，不绕审核。
- README 无需再次更新：本次为故障处理修正，不改变本轮已说明的本机编辑/云端审核边界。未部署、重置配额、修改云配置、合并或推送。
- 回归376/376通过，89文件语法、普通发布预检、TypeScript通过；新增审核失败防重复、请求无回调超时/abort、迟到登录不发请求及失败标题回归。新失败界面未做双端真机验收；云端成功成图未复测，不能声明根本额度限制已解除。

## 本机编辑剩余边界

- 首次分包下载仍需网络；原字体约11.1MiB，编码数据未压缩约14.9MiB。低端机解析内存、正式包下载、iOS/Android真机尚未验收；不能承诺首次秒开、永久缓存或清缓存后离线。
- 本机轮廓与云端字体抗锯齿/排版可能略有差异，特殊字符明确缺字提示；草稿清理或换设备不自动恢复。
- 云端最终审核45009仍待单独处理，本轮未调用保存接口消耗审核额度。未上线，生产NOT READY。

## fun-card-renderer-013 部署核验

- 基于当前工作树发布同一服务的新版本 `fun-card-renderer-013`，保留 `fun-card-renderer-012`；未新建服务、未改 VPC/子网/套餐/微信配置，未调用 `clearQuota`。
- 已在同一发布单配置定向 URL 参数灰度：`TrafficType=URL_PARAMS`，`gray013=1` 路由到 013，默认优先级保持 012；未改变百分比流量（012默认、013比例均为 0）。
- 013 部署状态为 `normal`；CPU/内存 `1/2`、端口 `8080`、最小/最大实例 `0/5`、VPC 字段与 012 一致；提交前后网络配置检查 `unchanged=true`。
- 当前流量仍为 012 `100%`，013 `0%`。已完成 013 容器构建/启动状态和三款内置字体 HTTP 200 检查；默认根路径返回预期业务 404，说明 8080 入口可达。
- 真正的微信开发者工具 `wx.cloud.callContainer` 调试输入未能执行，因此本轮没有产生可归属 013 的运行时请求；`/render-stack`、真实图片成图、`msg_sec_check` 真实调用及线上 `errmsg/rid/traceId` 日志字段未冒充通过，013日志仅含部署记录；按发布门禁未切流，等待人工/开发者工具认证冒烟。
- 本轮部署后的自动化回归仍为 379/379；语法、小程序预检、TypeScript、文档治理和 diff 检查通过。
