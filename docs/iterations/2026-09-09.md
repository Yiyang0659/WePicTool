# 2026-09-09 真实调用恢复，定位微信内容安全凭据错误

- 用户要求继续原 renderer 修复；保留既定网络、CDN、存储资源、合并与推送限制。
- 07:58（北京时间）运行微信开发者工具 `cli auto --project /Users/Zhuanz/Desktop/WePicTool/miniprogram --auto-port 9442`，工具确认 HTTP 服务端口 51181 已启动，但准备项目阶段返回 `需要重新登录（code 10）`。
- 此次未建立自动化连接，未发出新的 `wx.cloud.callContainer`，无新的云端状态码/requestId/图片；不将该登录错误归为 renderer 错误。
- 上轮已部署 v008 的平台状态证据、编辑器取图修复和测试结果见 2026-09-08 迭代记录。全量 310/312，两个本地启动期限测试未通过；其余预检通过。当前仍不能宣称整体修复完成。
- 下一步只需用户在微信开发者工具重新扫码登录，重新打开此项目并保持服务端口开启，再执行一次真实调用及对应云日志查询。本轮不重复整套测试、不再次部署。
- 用户随后确认登录后，CLI 曾成功返回 AppID `wxf16280c0ba5c507d` 与 `auto`；但在真实请求发出前任务多次中断。21:29 续接时临时自动化依赖已不存在，已定位到 npm 缓存里的同一工具；9442 无监听，重启自动化再次返回 `需要重新登录（code 10）`。本次仍没有发出真实渲染请求，不能沿用早前登录成功证明当前会话有效。

README 无需更新：本次仅续接验证并记录登录阻塞，无入口、功能或部署方式变化。

## 晚间登录恢复后的真实证据

### 23:50 暂停服务唤醒

- 用户报告已处理腾讯云平台小程序认证；不以此替代真实审核成功证据。
- 服务详情 API：normal；版本008流量100%；alwaysScale，MinNum=0，MaxNum=5；未发现需要重建服务的证据。
- 23:47:56 开发者工具真实探针返回 access_token missing（314ms），与此前服务器 invalid wx openapi access_token 分开记录，未证明请求进入容器。
- 现有公网域名 GET / 首次30.311s返回503；随后运行日志23:50:23显示容器 listening on 8080，再次 GET / 172ms返回404及 renderer 的 NOT_FOUND JSON。确认容器已唤醒，可处理请求；根路径没有页面，404不表示部署失败。
- 未重新部署、未修改最小实例/网络/CDN/存储或推送合并。最小实例0仍允许后续空闲缩容；持续保持至少1实例会增加持续费用，留待确认。
- 此次仅服务恢复冒烟，不代表三字体、内容安全和上传成功。文档检查与 diff 检查通过；按用户轻量验证要求未重跑全量测试。

- 21:32:06.731（北京时间）真实 `wx.cloud.callContainer` POST `/render-stack`，服务 `fun-card-renderer`，15,781ms 后返回 102002 请求超时，无 HTTP 状态或 requestId；callId 为 `1788960726784-0.6282776859688721`。
- 运行日志显示 v008 pod `fun-card-renderer-008-65556f9bbb-npx2z` 于 21:33:35 才输出 listening on 8080；提示冷启动风险，但没有关联请求追踪，不能认定为完整根因证据。
- 21:36:15.896 空对象路由探针在 261ms 返回预期 400 INVALID_REQUEST，证明请求可以到容器业务路由。
- 21:43:24.525 三字体固定中文测试请求在 308ms 返回 503 SAFETY_UNAVAILABLE，response requestId 缺失。
- 同容器 21:43:25 日志：audit-start；`content audit failed: openapi.security.msgSecCheck:fail -501001 resource system error. invalid wx openapi access_token`；audit-end kind=final status=503 elapsedMs=84。调用位置为 `miniprogram/cloudhosting/fun-card-renderer/index.js` 的 `cloud.openapi.security.msgSecCheck({ content })`。这是本次审核失败原始错误，不是字体失败或 OOM 的证据。
- 运行日志通过现有 tcbr 日志查询取得；日志查询管理 requestId 不冒充渲染 requestId。未创建日志或其他云资源。
- 本次未进入渲染和上传，三字体实际图片、上传成功、renderer 无字体 CDN 请求以及客户端 CDN 行为均未完成运行验证。
- 下一步最小方案：只读核对云环境与小程序关联和 OpenAPI 调用方式，再决定凭据/调用修复，不绕过审核，不改网络，不再次盲目部署。
- 本轮仅轻量真实调用与日志查询、状态文档更新，遵照用户要求不重跑深度测试；历史 310/312 结果保持，不声称全量通过。
