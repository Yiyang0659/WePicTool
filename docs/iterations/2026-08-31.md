# 2026-08-31 迭代日志

## 已完成

- 用户确认《分层云换装与内置模板系统设计说明》，新增对应实施计划并在 `codex/layered-dressup-mvp` 独立工作树执行。
- 新增玩法注册表：固定头像/发型、上衣、下装、鞋子四组，注册 `demo/upload/mixed` 三种入口和首套「抽象搞怪」素材包。
- 新增分层换装项目模型：支持空项目、模板项目、用户/系统混用、每组 12 张上限、删除、排序、首图更新、3 张阈值和多组预览契约。
- 使用内置 imagegen 生成三张项目自有的抽象纸娃娃头像素材；三张分别采用橘色短发、蓝色卷发和紫色双丸子头，均为 640×640 PNG，无文字、品牌、明星或第三方角色。最终文件为 `miniprogram/assets/samples/head1.png`、`head2.png`、`head3.png`。
- 新增 `pages/dressup`：支持直接试玩、恢复本地草稿、按部位上传、补系统素材、删除、左右排序、首图标记、预览、按组/全部保存、相册授权失败提示和四步发送引导。
- 修复试玩与上传入口的草稿串状态：上传入口只恢复 `upload/mixed` 草稿，忽略纯 `demo` 编辑记录。
- 首页升级为分层云换装旗舰入口，展示四个部位独立滑动效果、首套热门模板和两条入口；原 AI 穿搭整理保留为次级工具。
- 更新 README、当前状态、路线图、关键决策、更新摘要、PRD、玩法手册和技术规格，明确当前仅为功能分支实现，未合并、部署或真机验收。
- README 已更新：新增旗舰玩法、编辑器页面、注册表/项目模型、测试文件和当前真机验证边界。

## 验证

- TDD 红灯：注册表缺失、项目模型缺失、三张头像素材缺失、编辑页面/路由缺失、首页入口缺失时，对应新增测试均先出现预期失败。
- `npm test`：37 项通过，0 项失败。
- `npm run check:syntax`：通过，覆盖新增注册表、项目模型和编辑页。
- `npm run check:miniprogram`：通过。
- `npm run lint`：通过。
- `npm run check:docs`：通过。
- `git diff --check`：通过。

## 未完成与风险

- 尚未用微信开发者工具实际编译本分支，WXML/WXSS 表达式与基础库兼容性需要在开发者工具确认。
- 尚未完成 iOS/Android 真机验收；包内资源复制到用户目录、用户临时图片持久化、相册保存权限与保存顺序需要实测。
- 尚未在真实微信聊天中验证四组分别合并发送后的封面、顺序、独立滑动和展开/收起效果。
- 首版只有「抽象搞怪」一套内置模板；「日常基础」「甜酷风格」和清爽/编号输出模式仍在 P1。
- 分层换装首版不调用 AI；原 AI 穿搭工具仍受个人主体审核范围、云函数部署和隐私文案一致性约束。
- 三张头像是本次通过内置 imagegen 生成的项目资源；保留原始生成结果与本日志作为来源记录。现有上衣、下装、鞋子图片沿用仓库已有示例素材，其更早来源仍需在正式发布前核对。

## 下一步

1. 在微信开发者工具导入并编译 `codex/layered-dressup-mvp`，先解决任何页面兼容问题。
2. 用 iOS 和 Android 各走一遍直接试玩、用户上传、混合素材、删除排序、预览、保存和权限拒绝。
3. 在真实微信聊天中按头像/发型、上衣、下装、鞋子的顺序发送四叠并记录截图或录屏。
4. 真机验收通过后再决定合并，并开始 P1 的两套新模板与编号模式。
