# 2026-09-11 编辑页参考图实施

## 审核45009定位与预热故障止损

- 按systematic-debugging先取证。CLI只读012 normal/100%/8080/wechat-https；自动化重连成功，最初callWxMethod不支持嵌套cloud方法，改用evaluate真实wx.cloud.callContainer。22:55:24.566（北京时间）POST /preview-scene，490ms返回503 SAFETY_UNAVAILABLE，响应未取得requestId。
- CLI process日志仅部署流程；Chrome CUA连接超时。改用官方SearchClsLog只读检索：012容器22:55:26及22:40连续content audit failed: WECHAT_AUDIT_45009。查询requestId=24cd3075-773e-4ff5-9638-a9113cdbe6e6。代码wechatSafety.js将msg_sec_check非零errcode映射SAFETY_UNAVAILABLE，经server.js auditPayload返回503；不是CONTENT_UNSAFE，不跳过审核。
- 45009指向微信API调用限制；日额度/其他限制及恢复时间未从管理后台确认。预热字体变体逐张重复审核会放大用量，但未证明所有额度消耗来自本项目；单纯重新部署不能恢复平台额度。
- TDD新增故障队列测试：先失败3次实际请求对预期1次；最小客户端修复遇到审核不可用/限流/身份不可用停止未开始后台请求并暂停后续预热，缓存仍可读，前台手动请求成功后恢复。没有缓存“审核通过”或绕过审核。README 无需更新：已有预加载故障保护，不改入口和部署。
- 本轮未部署、修改云权限/网络、合并或推送；线上恢复需解除微信审核限制后单次轻量验证。
- 修复后367/367全量测试、63首方JS语法、普通小程序预检、TypeScript、文档治理及git diff --check通过。按verification-before-completion区分本地止损通过与真实服务仍受限；未继续发送审核请求消耗配额。

## 改字预览与保存按钮修订

- 本轮按bug修复例外执行。确认原预热占满两个请求位、组件额外400ms延迟；后台改为最多一个位，当前编辑提优且零额外延时。错误按身份、审核、限流、网络、图片读取分类显示，不输出敏感信息。前台新文字通过本地sceneValidator，但不代表实际云端审核通过。
- 自动化连接ws://127.0.0.1:9442失败，未取得截图对应callContainer和原始错误；云端失败根因及修复后的真实耗时未验证，不能宣称字体问题彻底恢复。
- 画布下保存按钮改24rpx字/64rpx高/限宽，右侧蓝色，底部按钮不变。README 无需更新：既有功能缺陷与局部视觉修正，不改变入口和部署步骤。
- 验证：完整366/366测试、63首方JS语法、普通预检、TypeScript、文档检查、git diff --check通过。新增改字替换预览、身份错误提示和后台让位回归；未部署、合并或推送，真机速度及按钮视觉待验收。

## 编辑保存按钮与后台预热

- 用户明确要求按顺序实现按钮调整和已讨论的预加载；先记录设计与实施计划再编码。保留画布和内容改字入口，替换提示行，增加相册当前页/整组保存，固定底部微信预览/完成；弹窗时隐藏固定栏，预留安全区。
- 保存与微信预览经render-stack审核和高清编号，当前页按sceneId定位；快照去重、失败解锁、相册拒绝授权提示及部分保存续传。首次单页保存沿用整组审核，不绕过审核也不使用编辑低清缓存。
- 候选确定后后台预热封面、卡片、三字体；编辑页优先当前方案。共享下载后的会话临时文件缓存，并发2、前台提优、排队任务取消、最多60项/15分钟；装饰不影响底图缓存，未知新组合首次等待不作零延迟保证。预热会增加已有renderer请求和流量，不是免费本地生成；不新增资源。
- 新增队列优先级、字体缓存键、当前页导出和审核失败回归。最终全量365/365测试、63首方JS语法、普通小程序预检、TypeScript、文档治理及git diff --check通过。真机相册权限、底部安全区、首次网络耗时仍未验证；普通预检仍提示正式发布前需独立验证公网隔离，本轮遵照用户要求未改公网配置。
- README 已更新：补充分支按钮与预加载行为、缓存边界。未部署、合并、推送或修改网络/CDN。

## 用户确认后交互实施与012部署

- 完成已确认设计对应计划。新10档笔宽4/8/12/16/24/28/40/56/72/96，前后端funStrokes完全同源；擦除分割相交线段、连续采样手势、不重连缺口、不用白色遮盖，超限保留上一有效状态。属于矢量线段橡皮，非像素纹理擦除。
- 新草稿2160工作区、1080视口，本机显式写画/平移模式；视口只改变本地显示，生成贴纸归一化完整工作区。旧草稿默认1080不放大/迁移。手写组保存工作区与视口以便再编辑，云端仍只接受1080坐标和审核后上传。
- 普通装饰一键置顶/置底，文字槽位不变，手写贴纸仍独立上层。保留上轮缩略Canvas卸载、固定画板和已添加边框修复。
- 全量361/361通过；62文件语法、普通预检、TypeScript、声明式发布预检、diff通过。定向8/8覆盖局部擦除/跨事件扫擦/点/身份/宽度/大工作区完整导出/旧数据/层级/平移取消。声明式预检不是私网安全或真机证据。
- 原服务部署提交requestId=8503f9c3-d606-414a-9b7d-827f2c14f619；提交后网络检查unchanged=true。只读确认fun-card-renderer-012，normal，100%，8080，镜像后缀012-20260911005336，微信HTTPS审核仍启用。没有创建资源、调整网络、合并或推送。
- 微信开发者工具auto返回code10需要重新登录；已有9442连接读到编辑页路径，但页面数据与截图失败。未执行012真实成图验证，不以部署normal冒充业务成功。已即时告知用户重新扫码登录，待轻量真机/成图验收。
- README 已更新：新交互、工作区、旧草稿和部署状态边界；治理文档同步。没有宣称上线小程序或真机通过。

## 手写缺陷跟进

- 当前修改：弹窗打开期间不挂载背景草稿Canvas，关闭后恢复，不删除素材；画布脱离scroll-view，固定于面板，仅工具区滚动，绘制层裁剪；已添加素材边框提高CSS优先级，选中底色/阴影独立保留。
- 代码确认普通样式覆盖已添加边框；缩略Canvas串层和滚动画布与截图现象一致，但未取得修复后真机证据，不能将代码推断当作真机根因证明。
- 新增3项结构回归；完整356/356测试、62首方JS语法、普通预检、TypeScript通过。结构回归不能证明原生Canvas合成和触摸效果。文档治理及diff检查单独执行。
- 根据治理要求，把改变原整笔擦除、1080固定工作区与逐层移动的行为写入待确认修订设计，尚未实现局部橡皮、平移、新笔宽或置顶置底。新增笔宽需前后端兼容，平移需明确完整作品导出范围，不以CSS假装实现。
- README 无需更新：本轮是已有界面显示修复，未增加可用功能、入口或部署步骤；新交互仅为待确认设计。未部署、推送、合并或修改云资源。

- 用户已确认保留上方画布并实现；先更新设计并编写实施计划，沿用codex/ai-outfit-picker-flow。保留画布/历史，下方分为排序、标签、手写卡、草稿库、贴纸卡、颜色/层级及完成按钮。原生系统栏不仿造。
- 新增独立本地手写面板、实线/荧光笔/整笔擦除、宽度/颜色、局部20步历史、取消与保存；草稿库最多10份/2MiB，实际笔迹缩略图和时间，长按删除单份/确认清空，独立于原项目草稿。
- 手写组保留原笔迹，可移动/双指缩放旋转/继续编辑/删除，项目撤销有效；前端转换为现有strokes协议，颜色和笔宽落入原白名单，超出画布拒绝变换，超过整卡笔迹限制拒绝添加。基础笔宽随缩放映射最近档位，不承诺连续压感笔刷。沿用011图片审核，无新协议部署、资源或网络更改。
- 开发者工具CLI重新连接成功，真实打开编辑页，localEditorEnabled=true；取得装饰页、独立手写面板截图并检查，发现按钮偏宽后修正源样式。手写面板显示实际测试笔迹，添加为贴纸后真实callContainer /render-stack返回200/ok及5张图片。此为正常审核成图冒烟，非风险图片/所有笔型/真机手势证明。
- 冒烟手写草稿从素材库精确删除（仅识别测试stroke_ui对应项），不删除用户其他草稿；测试项目留在编辑页供检查。本机素材库和项目草稿互不替代。未把测试缩略图当真实用户历史。
- 当前自动化353/353、62首方JS语法、普通小程序预检、TypeScript通过；文档、diff及声明式发布预检通过。最后自动重新导航报Uncaught，按钮宽度修正后的最终截图待手动编译复核。真实连续触摸、双指及窄屏逐像素视觉仍需用户验收，不能宣称已一比一验收完成。
- README 已更新：说明参考图操作区、独立手写库、矢量贴纸及本机边界。未合并主分支或推送GitHub。
