# 2026-09-06 迭代记录

## 趣味字画 P2.3 续验与编辑体验修正

用户要求继续执行已确认的趣味字画内容扩展与聚焦编辑计划，并在开发者工具实际操作后指出缩略状态不可辨识、排序拖动缺少其他卡片补位、文字弹窗被 Canvas 遮挡三项问题。本次沿用已确认的聚焦编辑范围，补充开发者工具证据并直接修正这些问题，没有增加新页面、无限图层或通用修图能力。

开发者工具检查：

- 在 iPhone 12/13 Pro 模拟尺寸成功打开趣味字画输入页；6 个分类和 12 个案例均正常显示，没有横向页面溢出；
- 点击“今天想见你”案例后，输入框回填“我今天想见你”，字数更新为 6/40，表达偏好随案例更新；切换表达标签也能写回页面状态；
- 点击真实生成入口时，云环境列表曾出现非阻断超时；内容审核不可用时仍按失败关闭规则处理，没有绕过审核生成；
- 用户完成登录后，内置示例成功生成三套候选；候选 A 横滑后由 `1/5` 变为 `2/5`，B/C 保持 `1/5`，证明三套滑动状态独立；
- 编辑器成功验证内容/样式/装饰三面板、代表性整组风格、背景、配色、字感、文字修改、撤销/重做、装饰添加和单指拖动；完成后结果页重新渲染并可进入微信效果预览；
- 修正后重新普通编译：顺序缩略项可读到“微信封面 + 装饰概貌 + 当前文字”，样式项显示“当前”和背景/配色勾选，装饰列表显示不同图形及“已添加”；打开文字弹窗后编辑页主 Canvas 从渲染树移除，关闭后恢复；
- 开发者工具普通编译没有页面编译失败；实施过程中发现并清除一条编辑页后代标签 WXSS 选择器告警。

编辑器改动：

- 卡片顺序缩略项基于当前场景生成轻量视觉样本，显示背景色、背景纹理类别、文字色、主要文字和已有装饰概貌；
- 风格、背景、配色、贴纸和涂鸦使用可辨识视觉样本，当前选中或已添加状态明确显示；
- 拖动卡片跨过其他槽位时，被跨过卡片立即移动到空位，`movable-view` 对非拖动项启用位置动画；松手仍只调用一次项目模型排序；
- 文字弹窗打开时暂时卸载底层 `fun-card-canvas`，遮罩和对话框使用独立高层级，避免微信 Canvas 压住白色内容框；
- “上移一层 / 下移一层”改为“往前显示 / 往后显示”，边界状态禁用并提示；说明文字明确该操作只影响装饰重叠时的遮挡，删除只作用于当前选中装饰；
- 排序尺寸优先使用 `wx.getWindowInfo`，减少新增的 `wx.getSystemInfoSync` 弃用调用。

本地 renderer 检查：

- 清理占用 8080 的旧 renderer 进程，按显式开发模式启动当前分支服务；
- `LXGWMarkerGothic-Regular.ttf`、`SmileySans-Oblique.ttf`、`MaShanZheng-Regular.ttf` 三条 `/font/` 路由均返回 HTTP 200、`content-type: font/ttf` 和 `access-control-allow-origin: *`；
- 这只证明本地字体网关与当前代码一致，不代替 CloudBase HTTPS、私密入口和双端字体加载验收。

完整验证：

```text
npm test
  288/288 通过，退出 0
npm run check:syntax
  51 个首方 JS 文件通过，退出 0
npm run check:miniprogram
  普通开发预检通过，退出 0；保留 access mode、HTTP 与 loopback 的上线提醒
npm run lint
  TypeScript 无错误，退出 0
npm run check:docs
  文档治理检查通过，退出 0
git diff --check
  通过，无空白错误
```

仍待外部验证：375 px 宽度、真实双指缩放/旋转、iOS、Android、CloudBase HTTPS/私密入口、系统相册和真实微信聊天。开发者工具的代表性样式验证与自动化的 7 风格/3 字感白名单覆盖不能替代这些真机结论。

README 无需更新：本次只修正既有聚焦编辑页的可见反馈、排序动画和弹窗遮挡，不改变功能名称、入口、运行方式或部署方式。`docs/changelog.md` 也无需更新，因为功能仍未合并或发布。

## 贴纸与涂鸦独立色板

用户在前述聚焦编辑范围内继续确认装饰改色，并提供了“圆角白色容器 + 横向圆形色点 + 方形选中底”的视觉参考。本次在用户确认的设计说明与实施计划上完成改动，没有增加新的一级入口或通用取色器。

实施结果：

- 新增鲜明色、浅色、透明色三组独立装饰色板，每组 8 色，共 24 色；
- 选中装饰后才能改色，改色只作用于当前贴纸或涂鸦；支持恢复原色、撤销和重做；
- 贴纸使用所选填充色与对比细节色，涂鸦使用所选描边色；透明色在色点下显示棋盘底纹理；
- 项目场景保存稳定的 `decorationColorKey`；缺省该字段的旧场景继续使用原粉色贴纸/黑色涂鸦，未知色键在本地与云端都被拒绝；
- 小程序 Canvas 和云端 renderer 各有独立白名单注册表，自动化校验颜色键、填充色与细节色完全一致。

开发者工具回归：

- 在 iPhone 12/13 Pro 模拟尺寸进入趣味字画示例的“自己改改”，切到装饰面板后添加爱心；
- 页面正确显示圆角色板框、鲜明/浅色/透明分类、8 个横向色点、恢复原色与装饰层级操作；
- 透明组色点显示棋盘底，点击透明紫后画布上当前爱心即时变色；小屏模拟器未发生横向页面溢出。

完整验证：

```text
npm test
  292/292 通过，退出 0
npm run check:syntax
  53 个首方 JS 文件通过，退出 0
npm run check:miniprogram
  普通开发预检通过，退出 0；保留 access mode、HTTP 与 loopback 的上线提醒
npm run lint
  TypeScript 无错误，退出 0
npm run check:docs
  文档治理检查通过，退出 0
git diff --check
  通过，无空白错误
```

仍待外部验收：375 px、iOS/Android 真机、CloudBase HTTPS 渲染和最终保存图的透明效果。README 无需更新：功能名称、入口、运行方式和部署方式都没有变化。`docs/changelog.md` 也无需更新，因为功能仍未合并或发布。

## 字体服务断连空白预览修复

用户在趣味字画编辑页遇到空白画布和 `loadFontFace:fail ... ECONNREFUSED 127.0.0.1:8080`。检查确认字体文件本身没有损坏，而是本地 renderer 进程已经停止、8080 端口无人监听；原编辑页只把字体组件错误原样弹出，没有接入候选页已有的服务端预览降级，因此整个画布留白。

修复结果：

- 按显式开发模式重新启动当前工作树的 `fun-card-renderer`；`LXGWMarkerGothic-Regular.ttf` 实测返回 HTTP 200，下载长度 3,179,616 字节；
- 编辑页监听字体不可用和 Canvas 渲染失败，失败时按当前项目请求服务端低清预览，并按候选与当前卡片索引显示对应图片；
- 切换卡片时同步切换降级图片；文字、样式、装饰或顺序发生变化后废弃旧预览，避免展示过期结果；
- 本地字体和服务端预览同时失败时，不清空项目、不伪装成系统字体，改为显示“手写预览暂时没连上”和“重新加载”；
- 候选页把 `fontunavailable` 接入已有 `onCanvasError`，字体错误和其他 Canvas 错误共用同一套只请求一次的服务端预览降级；
- 底层 `ECONNREFUSED` 不再作为用户提示展示。

开发者工具回归：

- 在 iPhone 12/13 Pro 模拟尺寸从首页进入趣味字画示例，再进入“自己改改”；
- 编辑页首张卡片重新显示“我本来想说”的手写内容，未出现空白画布或底层网络错误 toast；
- 开发者工具仍有既存的 `wx.getSystemInfoSync` 弃用警告，但本次链路没有新增页面编译或字体错误。

完整验证：

```text
npm test
  294/294 通过，退出 0
npm run check:syntax
  53 个首方 JS 文件通过，退出 0
npm run check:miniprogram
  普通开发预检通过，退出 0；保留 access mode、HTTP 与 loopback 的上线提醒
npm run lint
  TypeScript 无错误，退出 0
npm run check:docs
  文档治理检查通过，退出 0
git diff --check
  通过，无空白错误
```

仍待外部验收：生产环境必须把字体地址换成 CloudBase HTTPS 并完成私密入口和双端字体/服务端预览冒烟；本地 8080 恢复不代表生产门禁已解除。README 无需更新：功能名称、入口、运行和部署方式没有改变。`docs/changelog.md` 也无需更新，因为功能仍未合并或发布。

## 当前集成状态合入 main

用户明确要求把当前小程序状态合入主分支，以便继续测试生成效果并为后续上线做准备。本次只完成可恢复的主线集成和合并后验证，没有部署云函数/云托管、上传体验版、提交审核或发布。

合并结果：

- 合并前 `main` 为 `22b7a14`，根工作区干净；
- `codex/unified-stack-export` 的 96 个文件差异提交为 `0f55a38`（`feat: integrate shared stack experiences`）；
- 通过合并提交 `f52a8c8`（`merge: integrate shared stack experiences`）合入 `main`；
- `main` 现在包含分层云换装、趣味字画 P2.1/P2.3、共享首页、微信效果预览、统一导出、字体断连恢复、相关素材、测试与设计/实施文档；
- `README.md`、`docs/README.md`、`docs/current.md`、`docs/roadmap.md` 和 `docs/changelog.md` 已同步为“已合并、未部署、未发布”；来源分支保留为只读追溯点。

发布预检：

```text
npm run check:miniprogram:release
  退出 1（预期阻断）
  - 缺少 FUN_CARD_RENDERER_ACCESS_MODE=call-container-only
  - FUN_CARD_RENDERER_URL 仍为 HTTP
  - FUN_CARD_RENDERER_URL 仍指向 127.0.0.1 loopback
```

因此当前 `main` 可用于开发者工具和真机测试，但不能直接上传为正式发布包。正式发布前仍需完成 CloudBase 私密 renderer、HTTPS 字体精确网关、Docker/Linux、线上安全/渲染冒烟，以及 iOS、Android、系统相册和真实微信聊天验收。

合并后完整验证：

```text
npm test
  294/294 通过，退出 0
npm run check:syntax
  53 个首方 JS 文件通过，退出 0
npm run check:miniprogram
  普通开发预检通过，退出 0；保留 access mode、HTTP 与 loopback 的上线提醒
npm run lint
  TypeScript 无错误，退出 0
npm run check:docs
  文档治理检查通过，退出 0
git diff --check
  通过，无空白错误
```

回滚条件：若合并后的主线出现阻断性回归，应从 `main` 新建修复/回滚分支并优先修复；必须整体撤回时，可对合并提交 `f52a8c8` 执行非破坏性 revert，合并前基线 `22b7a14` 保留不变。不得使用 `reset --hard` 覆盖用户工作。

README 已更新：继续开发入口、主线状态和已实现能力范围从历史功能分支切换为 `main`。`docs/changelog.md` 已更新 Unreleased：记录已合并但尚未发布的用户可感知能力。

## AI 穿搭空选图与分层换装单图预览

用户确认把两个首页流程合并实施：更多工具中的“AI 穿搭整理”先进入空选图状态；共享预览中的“分层换装”四个部位各显示一张放大素材，通过手势或箭头切换，右侧按穿搭顺序竖向汇总。改动位于新分支 `codex/ai-outfit-picker-flow`，尚未合入 `main`、部署或发布。

实施结果：

- AI 穿搭入口不再立即弹出系统相册，而是进入“还未选择图片”的空状态；中央“添加图片”才调用 `wx.chooseMedia`；
- 空状态禁止开始处理；选择后支持继续追加、单张删除和大图预览，最多 9 张；
- 左上返回清空本次临时选择并回首页，右上“重新选择”只清空图片并留在选图状态，两者不再共用处理器；
- 首页头像/发型、上衣、下装、鞋子四行分别使用原生循环 swiper，静止时只展示一张放大图片；左右箭头复用同一索引更新；
- 右侧“当前选择”从 2×2 网格改为按头像/发型、上衣、下装、鞋子竖向排列，并随左侧单行切换同步；
- “上传我的素材”继续进入 `/pages/dressup/dressup?mode=upload`，没有修改四组素材管理和导出协议；新增自动化逐一验证四个部位均可独立接收用户图片。

开发者工具检查：

- 普通编译成功，没有新增页面编译错误；
- 可访问性结构确认首页存在四个横向滑动列表、四个部位计数、右侧四个顺序标签，素材页仍存在四个“上传我的”入口；
- 当前桌面控制截图停留在旧页面缓存，未得到可作为视觉验收证据的新版截图，因此不把本轮写成尺寸或手势验收通过。

实施后验证：

```text
node --test tests/layered-dressup.test.cjs
  25/25 通过，退出 0
npm test
  300/300 通过，退出 0
npm run check:syntax
  53 个首方 JS 文件通过，退出 0
npm run check:miniprogram
  普通开发预检通过，退出 0；保留 access mode、HTTP 与 loopback 的上线提醒
npm run lint
  TypeScript 无错误，退出 0
npm run check:docs
  文档治理检查通过，退出 0
git diff --check
  通过，无空白错误
```

仍待外部验收：375 px 实际版式、四行横向手势与页面纵向滚动的冲突、iOS/Android 相册授权、四部位真实图片读取，以及 AI/CloudBase 后续处理。README 已更新：用户可感知的首页入口、选图旅程和分层预览交互发生变化。`docs/changelog.md` 无需更新：当前功能分支尚未合入或发布。
