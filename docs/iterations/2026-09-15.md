# 2026-09-15 主分支固定到桌面目录

- 用户明确要求将4695的miniprogram替换桌面同名目录并移除旧路径。本轮按该指令在main整理入口，不新建功能分支，不改变产品行为。
- 开始时桌面为caef7f0/codex/ai-outfit-picker-flow，4695为3c3474e/main；两者没有已跟踪文件的未提交修改。先完整归档4695，保存桌面私有配置和本机环境文件；备份目录 `/Users/Zhuanz/WePicTool-Backups/consolidate-20260915-ta2A0z`，归档通过gzip完整性检查，包含私有配置与本机环境文件，权限600。
- 4695先释放main，桌面切换main；按用户后续明确指令将桌面miniprogram移入备份，4695/miniprogram整体移动到桌面。4695的私有配置/依赖随目录保留，renderer环境文件复制到桌面，旧桌面环境文件仍在备份，不输出密钥。已备份的4695剩余工作区通过git worktree remove移除；非指定其他工作区与分支保留，远端不删除。
- 替换后 `git diff --exit-code main -- miniprogram` 零差异；459/459测试、96文件语法、小程序预检、TypeScript、文档检查及diff检查通过。测试日志 `/tmp/wepic-replaced-main-tests.log`。miniprogram/project.private.config.json仍是未跟踪本机配置，不计入代码一致性结论。
- README 已更新：今后只使用 `/Users/Zhuanz/Desktop/WePicTool/miniprogram`；current同步当前入口。没有业务代码变更，无需新增产品变更摘要。
- 未调用真实AI、相册、微信审核或部署；静态预检不证明公网关闭或真机功能通过。用户已明确原提审使用旧桌面目录，本轮不替换平台审核包，重新上传由用户后续进行。
- 回退可从本地归档提取4695文件，旧功能分支caef7f0仍保留；不重写Git历史，不推送。
