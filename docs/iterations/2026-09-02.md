# 2026-09-02 迭代记录

## 本次目标

交付「趣味字画」P2.2 阶段核心能力——接入生成式大语言模型（LLM）作为智能故事规划器（`creativePlanner`），并建立严校验、单次带错自我修复与无缝规则静默兜底的高可用闭环；同时解决本地微信开发者工具调试时字体分发与渲染端点连接问题。

## 完成内容

1. **Prompt 工程与 JSON 解析器**：实现 `miniprogram/utils/storyPrompt.js`，定义面向微信叠图滑动节奏的系统 Prompt、Few-Shot 引导及合规 JSON 提取解析器。
2. **候选计划严格校验与修复**：增强 `miniprogram/utils/candidateValidator.js`，支持 AI 模式下的原意保留与字数边界检测，并提供 `buildRepairPrompt` 单次自动修复逻辑。
3. **云端故事规划服务**：实现 `miniprogram/cloudfunctions/planFunTextStory` 云函数，支持模型请求、8 秒超时保护、一次自动重试与聚合文字二次内容安全审核。
4. **客户端 AI 调度与规则降级闭环**：实现 `miniprogram/utils/creativePlannerClient.js`，优先调用云端大模型，在任何异常时无感降级为已有规则规划器，确保 100% 成功率。
5. **输入页体验打通**：更新 `pages/fun-text`，将一句话输入与表达标签接入 AI 故事规划器，并完成候选项目生成。
6. **本地调试与离线渲染增强**：在 `miniprogram/cloudhosting/fun-card-renderer/index.js` 中增加本地开发模拟模式，支持本地手写字体分发与 1080P PNG 渲染，并在 `miniprogram/config/env.js` 中配置 `http://127.0.0.1:8080` 便于微信开发者工具直接调试。
7. **自动化测试套件**：新增 `tests/fun-text-story-prompt.test.cjs`、`tests/fun-text-ai-validator.test.cjs`、`tests/cloud-plan-story.test.cjs`、`tests/creative-planner-client.test.cjs`，全量测试用例数扩充至 147 项。

## 验证结果

2026-09-02 在 `/Users/Zhuanz/Desktop/WePicTool/.worktrees/fun-text-stack-phase1` 执行：

```text
npm test                      通过，147 项测试全部 PASS (0 失败)
npm run check:syntax          通过，全部 43 个核心文件语法检查通过
npm run check:miniprogram     通过，小程序上线预检通过
npm run lint                  通过，TypeScript 类型检查无错误
npm run check:docs            通过，文档治理检查通过
git diff --check              通过
```

README 无需更新：本次为 P2.2 AI 故事规划器与本地调试增强实施，未改变小程序整体对外入口结构或项目架构定位。

## 遗留风险

- 真实线上生产环境需部署 `planFunTextStory` 云函数并配置 `LLM_API_KEY`（或 DashScope API Key）。
- 云托管 HTTPS 域名上线前需在微信公众平台配置为合法 request 域名并填入 `env.js`。

## 下一步

1. 推进 P2.3 轻量自由编辑扩展（单卡字感、12 种贴纸与 6 种涂鸦自由增删）。
2. 在微信开发者工具与真机环境下完成双端多场景输入冒烟验证。

## 趣味字画阶段一 Task 6 Fix Round 1

### 修复内容

1. **视觉场景严格校验**：每个文字层必须显式携带四个已注册 `effectKey` 之一；背景对象只接受 `{ assetKey, color }`，必须包含已注册且与 `stylePackId` 对应的背景键。缺失、未知、跨视觉包和额外图片/渐变形式均在审核或绘制前返回 `INVALID_REQUEST`。实际 PNG 画笔同步移除 `marker-bold` 静默默认值，直接调用时遇到缺失/未知效果也会失败。
2. **三候选预览请求级回滚**：`/preview-stack` 记录本次请求内每套已上传卡片；候选 2/3 的绘制、上传或响应顺序/身份校验失败时，除当前候选由场景渲染器清理的部分上传外，还会删除此前候选的全部上传。清理为尽力执行，清理自身异常不覆盖稳定的 `RENDER_FAILED` 响应。
3. **内容审核错误码收敛**：云托管生产审核与既有 `contentGuard` 一致，仅微信官方 `errCode=87014` 映射为 `CONTENT_UNSAFE` / 403；权限、配额、配置、未知 API 响应、空响应和抛错均映射为 `SAFETY_UNAVAILABLE` / 503。本地开发离线审核行为保持不变。
4. **超限 HTTP 请求稳定响应**：超过 2 MiB 后不再销毁请求 socket；服务停止累积正文但继续排空输入，在 `end` 后稳定返回 JSON `INVALID_REQUEST` / 400。

### TDD 与聚焦验证

2026-09-02 在 `/Users/Zhuanz/Desktop/WePicTool/.worktrees/fun-text-stack-phase1` 实际执行：

```text
node --test --test-name-pattern="rejects malformed|without an explicit registered effect" tests/fun-card-renderer.test.cjs
RED：2 项失败；缺失 background.assetKey 仍通过校验，缺失 effectKey 仍由 marker-bold 默认值成功绘制

node --test --test-name-pattern="preview rollback" tests/fun-card-renderer.test.cjs
RED：1 项失败；候选 2 渲染失败后此前候选删除数为 0，预期 3

node --test --test-name-pattern="official unsafe" tests/fun-card-renderer.test.cjs
RED：1 项失败；生产审核映射边界尚未导出，createContentChecker 为 undefined

node --test --test-name-pattern="oversized HTTP" tests/fun-card-renderer.test.cjs
RED：1 项失败；真实 fetch 收到 socket close，报 TypeError: fetch failed

node --test tests/fun-card-renderer.test.cjs
GREEN：16 项通过，0 失败

node --test tests/fun-text-story-prompt.test.cjs tests/fun-text-ai-validator.test.cjs tests/cloud-plan-story.test.cjs tests/creative-planner-client.test.cjs
通过，11 项 P2.2 Prompt、AI 校验、云规划和客户端降级测试通过，0 失败

npm --prefix miniprogram/cloudhosting/fun-card-renderer test
通过，16 项通过，0 失败

npm run check:syntax
通过

npm test && npm run check:syntax && npm run check:miniprogram && npm run lint && npm run check:docs && git diff --check
通过，151 项测试通过、0 失败；语法检查、小程序上线预检、TypeScript、文档治理与差异空白检查均通过
```

README 无需更新：本轮只收紧既有云托管内部校验、审核错误映射、请求回滚和 HTTP 错误响应，不改变项目入口、开发命令、部署方式或用户可访问功能范围。

## 趣味字画阶段一 Task 6 Fix Round 2

### 修复内容

`fun-card-renderer` 的生产内容审核响应不再通过 `Number(...)` 宽松转换错误码。现在只有响应自身显式包含数值型 `errCode=0` 才通过，数值型官方 `87014` 才映射为 `CONTENT_UNSAFE` / 403；缺失、`null`、空字符串、字符串形式错误码、非数字文本及未知数值全部映射为 `SAFETY_UNAVAILABLE` / 503。本地开发离线审核路径保持不变。

### TDD 与聚焦验证

2026-09-02 在 `/Users/Zhuanz/Desktop/WePicTool/.worktrees/fun-text-stack-phase1` 实际执行：

```text
node --test --test-name-pattern="official unsafe" tests/fun-card-renderer.test.cjs
RED：1 项失败；{ errCode: null } 被宽松转换为成功，继续渲染后实际返回 RENDER_FAILED / 500，预期 SAFETY_UNAVAILABLE / 503

node --test --test-name-pattern="official unsafe" tests/fun-card-renderer.test.cjs
GREEN：1 项通过，覆盖数值型成功码、官方不安全码以及 null、空字符串、缺失、字符串和非数字错误码

node --test tests/fun-card-renderer.test.cjs
通过，16 项渲染器测试通过，0 失败

node --test tests/fun-text-story-prompt.test.cjs tests/fun-text-ai-validator.test.cjs tests/cloud-plan-story.test.cjs tests/creative-planner-client.test.cjs
通过，11 项 P2.2 Prompt、AI 校验、云规划和客户端降级测试通过，0 失败

npm --prefix miniprogram/cloudhosting/fun-card-renderer test
通过，16 项通过，0 失败

npm test && npm run check:syntax && npm run check:miniprogram && npm run lint && npm run check:docs
通过，151 项测试通过、0 失败；语法检查、小程序上线预检、TypeScript 与文档治理检查全部通过
```

README 无需更新：本轮只修正既有生产内容审核响应的类型边界，不改变项目入口、开发命令、部署方式或用户可访问功能范围。

## 趣味字画阶段一 Task 8 Fix

### 修复内容

1. 候选页服务端低清预览增加按 `projectId` 维护的自动回退闩锁。一次自动 Canvas 错误请求无论成功或失败均不会被同一项目后续 `rendererror` 重复触发；`initProject` 和“再来三套”重建项目会重置闩锁，失败后的“点此重试”会显式清除闩锁后重新请求。
2. `funCardRendererClient` 现在会在返回前去除每张卡片 URL 的首尾空白，且只接受 `https://`、本地调试所需的 `http://` 和 `cloud://`；空白、`javascript:`、`data:`、`file:` 与其他协议均关闭式返回 `INVALID_RENDER_RESPONSE`。
3. 除明确的 `CONTENT_UNSAFE` 和 `SAFETY_UNAVAILABLE` 外，所有 HTTP 非 200 或 `ok !== true` 服务端响应统一归一为 `INVALID_RENDER_RESPONSE`；传输失败仍为 `NETWORK_ERROR`。
4. 结果页顺序保存测试的模拟渲染地址改为允许的本地 HTTP 地址，并模拟下载 API；未修改 P2.2 或本地 Canvas 导出的生产行为。

### TDD 与验证

2026-09-02 在 `/Users/Zhuanz/Desktop/WePicTool/.worktrees/fun-text-stack-phase1` 实际执行：

```text
node --test tests/fun-text-candidates-page.test.cjs
RED：17 项中 4 项失败；支持 URL 未去空白、空白/不安全协议未拒绝、RENDER_FAILED 错误码泄漏，以及成功降级后第二个 rendererror 发出第 2 次请求

node --test tests/fun-text-candidates-page.test.cjs tests/fun-card-canvas.test.cjs
GREEN：27 项通过，0 失败

node --test tests/fun-text-candidates-page.test.cjs tests/fun-card-canvas.test.cjs tests/fun-text-result.test.cjs tests/fun-text-entry.test.cjs
通过，39 项候选页、画布、结果页与入口回归测试通过，0 失败

npm test
通过，156 项测试通过，0 失败
```

README 无需更新：本轮仅修正既有候选低清预览降级与渲染响应边界，不改变小程序入口、开发命令、部署方式或已声明的用户功能范围。

## 趣味字画阶段一 Task 9 Review Fix

### 根因与修复内容

复核发现轻编辑页仍以左右箭头调用 `onMoveCard` 排序，未实现 Task 9 声明的 `movable-area` / `movable-view` 拖动生命周期；文字 UI 仍绑定旧的 `onStartEditText` / `onConfirmEditText` 名称。

1. 缩略图改为真实 WXML `movable-area` / `movable-view` 绑定。`onSortStart` 记录来源索引，`onSortMove` 以拖动缩略图中心与所有缩略图中心的最近距离计算目标索引，`onSortEnd` 清理生命周期状态后才调用一次 `funTextProject.moveCard`；重复结束事件和移动事件均不会再造成二次排序。
2. 排序后重建缩略图坐标和标签，第三张拖到第一张会得到连续 `order`，第一张的运行时标签保持“微信封面”。
3. 文字 UI 和页面 API 统一为 `onEditText`、`onConfirmText`，保持既有字数限制、视觉包切换和结果页导航行为。
4. 页面测试从实际 WXML 读取绑定的处理器，再驱动 `start → move → end` 生命周期；覆盖移动过程零次调用、结束后一次调用、重复结束不重复调用、顺序连续和封面标签。

### TDD 与验证

2026-09-02 在 `/Users/Zhuanz/Desktop/WePicTool/.worktrees/fun-text-stack-phase1` 实际执行：

```text
node --test tests/fun-text-editor-page.test.cjs
RED：7 项中 4 项失败；WXML 仍绑定 onStartEditText、页面缺少 onEditText，且不存在 movable-view 生命周期绑定。

node --test tests/fun-text-editor-page.test.cjs && npm run check:miniprogram
GREEN：7 项通过，0 失败；小程序上线预检通过。

node --test tests/fun-text-editor-page.test.cjs tests/fun-text-project.test.cjs && npm run check:syntax
通过：21 项页面与项目回归测试通过，0 失败；语法检查通过。

npm test && npm run check:syntax && npm run check:miniprogram && npm run lint && npm run check:docs && git diff --check
通过：156 项测试通过，0 失败；语法检查、小程序上线预检、TypeScript、文档治理与差异空白检查均通过。
```

README 无需更新：本轮是既有轻编辑页的交互契约修复，不改变小程序入口、运行方式、部署方式或项目整体定位。

## 趣味字画阶段一 Task 9 Fix Round 2

### 修复内容

`onConfirmText` 此前仅刷新主画布使用的 `scenes` 与 `currentScene`，缩略图 `sortItems` 仍保留更新前的浅拷贝场景，导致确认文字后缩略图文案滞后。本轮从更新后的候选场景重建 `sortItems`、宽度和区域宽度；不修改 `draggingIndex`、`sortFromIndex` 或 `sortToIndex`，因此未完成的拖拽生命周期状态保持原值，封面标签与缩略图几何也保持稳定。

### TDD 与验证

2026-09-02 在 `/Users/Zhuanz/Desktop/WePicTool/.worktrees/fun-text-stack-phase1` 实际执行：

```text
node --test tests/fun-text-editor-page.test.cjs
RED：8 项中 1 项失败；确认文字后第一个缩略图仍显示“我本来想说”，预期“先等等”。

node --test tests/fun-text-editor-page.test.cjs tests/fun-text-project.test.cjs && npm run check:miniprogram
GREEN：22 项页面与项目回归测试通过，0 失败；小程序上线预检通过。

npm test && npm run check:syntax && npm run check:miniprogram && npm run lint && npm run check:docs && git diff --check
通过：157 项测试通过，0 失败；语法检查、小程序上线预检、TypeScript、文档治理与差异空白检查均通过。
```

README 无需更新：本轮只同步既有编辑页的派生缩略图状态，不改变小程序入口、运行方式、部署方式或项目整体定位。

## 趣味字画阶段一 Task 10 审查修复

### 根因与修复内容

1. `template-result` 此前把 `requestRenderStack` 的所有异常都送进本地 Canvas，导致 `CONTENT_UNSAFE`、`SAFETY_UNAVAILABLE` 与 `INVALID_RENDER_RESPONSE` 也能绕过关闭式服务端门禁。本轮改为错误码白名单：只有既有客户端契约中的 `FUN_RENDERER_NOT_CONFIGURED` 和 `NETWORK_ERROR` 才允许本地降级；其余错误进入失败态，不生成任务、不写历史记录。
2. `imageExporter.resolveImagePath` 此前只要 `downloadFile` 返回 `tempFilePath` 就视为成功。本轮在使用临时路径前拒绝 HTTP 非 2xx 响应，并稳定返回 `DOWNLOAD_FAILED`。
3. `dressup` 迁移共享导出器时没有把原页面的路径解析步骤接进新循环，导致 `/assets/...` 内置素材直接传给相册 API。本轮为共享顺序导出增加单项路径解析钩子，穿搭页复用原 `resolveImageFilePath`，恢复逐张复制到 `wx.env.USER_DATA_PATH/layered-dressup` 后再保存，同时保留云文件、网络文件、保存进度和既有提示行为。
4. 趣味字画预览组不再使用候选标题，`groups[0].name` 固定为“趣味字画”，卡片与页面比例仍为 `1:1`。
5. `result.js` 的未调用旧下载/保存方法未删除，以避免在本轮关闭式安全和兼容修复中扩大改动范围。

### TDD 与验证

2026-09-02 在 `/Users/Zhuanz/Desktop/WePicTool/.worktrees/fun-text-stack-phase1` 实际执行：

```text
node --test tests/image-exporter.test.cjs tests/fun-text-result.test.cjs tests/fun-text-project.test.cjs tests/layered-dressup.test.cjs
RED：40 项中 35 项通过、5 项失败；候选组名仍为方案标题，安全错误仍触发 Canvas，HTTP 404 临时路径仍被接受，内置资源未复制到 USER_DATA_PATH。

node --test tests/image-exporter.test.cjs tests/fun-text-result.test.cjs tests/fun-text-project.test.cjs tests/layered-dressup.test.cjs
GREEN：40 项通过，0 失败。

node --test tests/image-exporter.test.cjs tests/fun-text-result.test.cjs tests/layered-dressup.test.cjs && npm run check:miniprogram
通过：26 项 Task 10 与穿搭回归测试通过，0 失败；小程序上线预检通过。

npm test && npm run check:syntax && npm run check:miniprogram && npm run lint && npm run check:docs && git diff --check
通过：161 项测试通过，0 失败；语法检查、小程序上线预检、TypeScript、文档治理与差异空白检查均通过。
```

README 无需更新：本轮只收紧既有结果渲染失败边界、共享下载状态检查、内置资源保存兼容和预览组名，不改变项目入口、运行方式、部署方式或对外功能范围。

## 趣味字画阶段一 Task 10 审查修复 Round 2

`template-result` 的本地高清降级白名单此前使用普通对象下标访问，会把原型链上的 `constructor`、`toString`、`__proto__` 当作真值并错误打开 Canvas 本地降级。本轮改为字符串错误码加 `Object.prototype.hasOwnProperty.call` 的精确自有白名单；仅 `FUN_RENDERER_NOT_CONFIGURED` 与 `NETWORK_ERROR` 可降级，原型链和未知码均保持关闭式失败，且不生成历史记录。

2026-09-02 在 `/Users/Zhuanz/Desktop/WePicTool/.worktrees/fun-text-stack-phase1` 实际执行：

```text
node --test tests/fun-text-result.test.cjs
RED：5 项中 4 项通过、1 项失败；`constructor` 错误码错误发起 1 次 Canvas 查询，预期为 0。

node --test tests/fun-text-result.test.cjs
GREEN：5 项通过，0 失败；`constructor`、`toString`、`__proto__` 均确认无 Canvas 降级、无任务和无历史记录。

node --test tests/image-exporter.test.cjs tests/fun-text-result.test.cjs tests/fun-text-project.test.cjs tests/layered-dressup.test.cjs && npm run check:miniprogram
通过：40 项聚焦 Task 10 / 项目 / 穿搭回归测试通过，0 失败；小程序上线预检通过。

npm test && npm run check:syntax && npm run check:miniprogram && npm run lint && npm run check:docs && git diff --check
通过：161 项测试通过，0 失败；语法检查、小程序上线预检、TypeScript、文档治理与差异空白检查均通过。
```

README 无需更新：本轮只纠正既有渲染错误码白名单的原型链安全边界，不改变项目入口、运行方式、部署方式或对外功能范围。

## 趣味字画阶段一 Task 11 审查修复

### 根因与修复内容

1. `template-result` 过去只写 `wepic_history_tasks`，记录页实际读取的 `wepictool_records` 因而没有趣味字画结果；现在真实写入记录，并以 `projectId`（兼容旧 `taskSnapshot.taskId`）upsert，保留原创建时间且限制为 20 条。
2. 临时卡片缓存过去只要非空即被复用；现在仅当每张卡片按顺序与选中 `editedScenes` 的 sceneId、role、order 完整一致且 URL 非空时才复用，否则重新渲染。
3. 趣味字画记录过去同时发送 `funTextProject` 和 `acceptTaskData`，令结果页重复初始化；现在只发送一个事件。
4. 记录路由现在明确处理 `outfit`、`dressup`、`layered-dressup`、`funtext` 与 `bigtext`。未知类型仅提示“该记录版本暂不支持”，不导航、不删除；旧大字提示固定为“旧大字滑卡记录暂不支持直接打开，请重新制作”，不再宣称已发布。`funtext` 只接受 version 1，未知版本不渲染也不写记录。

### TDD 与验证

2026-09-02 在 `/Users/Zhuanz/Desktop/WePicTool/.worktrees/fun-text-stack-phase1` 实际执行：

```text
node --test tests/fun-text-result.test.cjs tests/fun-text-record.test.cjs
RED：14 项中 8 项通过、6 项失败；结果页未写 wepictool_records、未 projectId upsert、残缺缓存未重渲染、version 2 仍渲染；记录页有双 event、旧大字文案不匹配、layered-dressup 路由错误、未知记录仍导航。

node --test tests/fun-text-record.test.cjs
RED：5 项中 1 项通过、4 项失败；确认记录侧四项缺口可稳定复现。

node --test tests/fun-text-result.test.cjs tests/fun-text-record.test.cjs
GREEN：14 项通过，0 项失败。

npm test && npm run check:syntax && npm run check:miniprogram && npm run lint && npm run check:docs && git diff --check
通过：167 项测试通过，0 失败；语法检查、小程序上线预检、TypeScript、文档治理与差异空白检查均通过。
```

README 无需更新：本轮只修复既有本地记录恢复、缓存复用与兼容提示边界，不改变小程序入口、运行方式、部署方式或已声明的对外功能范围。

## 趣味字画阶段一 Task 11 审查修复 Round 2

### 根因与修复内容

1. 第一轮仅凭 sceneId、role、order 和非空 URL 复用缓存，无法证明卡片属于当前渲染内容。本轮新增对实际渲染输入的确定性稳定指纹，写入 task、record 与每张 card；复用时重新计算并逐层核对。URL 先 trim 且仅允许 `https://`、`http://`、`cloud://`，旧无指纹缓存和临时/脚本/内联/本地地址全部重渲染。
2. 记录恢复现在以顶层 `projectSnapshot` 为基准。仅当 task 是有效 funtext task、taskId 与项目一致，且 task 内项目与顶层项目渲染指纹相同时单次发送 `acceptTaskData`；否则顶层项目有效时只发送 `funTextProject`。
3. 记录 upsert 只匹配 `type === 'funtext'` 且全部显式身份字段为一致非空字符串的 canonical identity。outfit/其他类型 ID 碰撞、funtext 内部冲突和畸形身份都保留原记录并安全新增。
4. `template-result` 为每次初始化生成 request generation，并在远端成功/失败、本地 Canvas callback、逐卡导出 await 前后与最终落库前核对；pending 状态忽略重试点击，旧项目或旧重试无法覆盖新成功与本地记录。

### TDD 与验证

2026-09-02 在 `/Users/Zhuanz/Desktop/WePicTool/.worktrees/fun-text-stack-phase1` 实际执行：

```text
初始聚焦 RED：22 项中 11 项通过、11 项失败；稳定复现四类审查缺口。
增量 RED：pending 重试、对象键序稳定指纹、畸形显式身份各 1 项独立失败；代码审查追加的旧记录缺失分层指纹、重复 canonical funtext 未完全 upsert 两项也各自独立失败。

node --test tests/fun-text-project.test.cjs tests/fun-text-result.test.cjs tests/fun-text-record.test.cjs
GREEN：41 项通过，0 项失败。

npm test && npm run check:syntax && npm run check:miniprogram && npm run lint && npm run check:docs && git diff --check
通过：180 项测试通过，0 失败；语法检查、小程序上线预检、TypeScript、文档治理与差异空白检查均通过。
```

README 无需更新：本轮继续收紧既有 Task 11 的缓存归属、记录身份与异步竞态边界，不改变项目入口、运行方式、部署方式或已声明的对外功能范围。
