# 2026-09-13 013剩余验收检查

## 保存鉴权错误定位补充（未解决线上阻塞）

- 用户11:41截图仍为身份验证失败。客户端每次保存调用wx.login后传入服务端，未发现复用code；服务端通过jscode2session验证，当前旧提示合并403与503，截图不足以确认具体失败原因。
- 客户端分别提示登录凭证拒绝/验证服务不可用，安全日志仅含错误码、HTTP状态、路径、服务名、时间及格式校验后的请求ID，不输出header、登录code、AppSecret或作品。服务端补充原先静默的HTTP/连接/响应/超时错误类别，鉴权失败仍关闭式拒绝，不绕过审核。
- 本地459/459、96文件语法、小程序静态预检、TypeScript、文档检查及git diff --check通过，日志`/tmp/wepic-auth-diagnostics-tests.log`。未部署服务端、未操作云端或真机测试；具体线上原因待安全诊断日志，不将测试通过表述为保存已恢复。
- README 无需更新：仅错误分类及诊断信息补充，未改变产品入口或鉴权架构。

## 历史记录单项容量超限

- 用户日志明确指出template-result两个记录写入均报entry size limit reached；原逻辑将20份含项目快照的记录塞入单一Storage项。新增recordStorage，小记录保持原格式，大记录按64000字符拆分；完整写入后发布索引，失败仅清理新分块，旧记录不被覆盖。
- template-result/result、record及profile统一读写和删除，兼容旧数组，不要求清缓存；不削减手写数据或原作品。存储总容量不足仍可能失败，不宣称无限容量或修复微信身份验证。
- 验证457/457、96文件语法、小程序静态预检、TypeScript、文档检查与git diff --check通过；回归覆盖单项超限复现、中文/emoji完整往返、顺序/笔迹保持、失败回滚及旧记录兼容/分块清理。日志`/tmp/wepic-record-storage-tests.log`。
- README 无需更新：内部记录存储修复，不改变使用入口或部署方式。未清除用户数据、未操作云端或做真机功能测试。

## 手写场景缺失尺寸修复

- 真机截图paintScene“场景必须为1080方图”追溯到handwritingLibrary.scene：手写面板和草稿缩略图场景缺失width/height。补齐1080逻辑尺寸，不缩小2160笔迹工作区，不变更视口、内容或持久草稿。
- 新回归直接调用真实paintScene覆盖空手写面板、旧草稿默认场景、有背景扩展工作区草稿；修复前复现相同报错，修复后全量454/454、95文件语法、小程序静态预检、TypeScript、文档检查与git diff --check通过。测试日志`/tmp/wepic-handwriting-dimensions-tests.log`。
- README 无需更新：仅修复内部场景尺寸契约，入口、配置和部署方式不变。未操作云端、未生成预览包或做功能验收；微信身份验证报错仍需独立定位，不能以本次修复视为解决。

## 七张卡滑动与保存误拦截修复

- 本地验证：全量453/453通过，95文件语法、小程序静态预检、TypeScript、文档检查和git diff --check通过；日志`/tmp/wepic-full-stack-tests.log`。不包含用户功能测试。
- 代码确认预览仅创建三个节点并永久轮换原图片；新增完整卡片索引，三节点分别显示当前/下一张/上一张，展开重排后同步索引。回归覆盖7张完整循环、反向滑动、展开收起继续滑动。
- 去除自由卡保存的独立能力预查询，直接调用render-stack；服务端的场景校验、身份验证、内容审核及客户端返回卡片完整性检查仍保留。INVALID_REQUEST单独提示，不再将无效能力响应概括为“服务未更新”。实际截图请求命中哪个版本未取证，不能声称解决全部线上路由问题。
- README 已更新：取消能力预查询后的实际保存流程。本轮按用户要求仅代码修复与本地检查，无云端操作、预览包或功能验收。

## 保存服务候选016部署

- 用户在本地私密文件补齐微信AppSecret并授权继续。部署计划校验只提交renderer源码与微信审核环境变量，不提交网络字段；`.env.renderer.local`权限为600且被Git忽略，密钥未输出到日志或源码包。
- 首次GRAY提交被旧发布单占用并返回ResourceInUse，未创建版本。用户明确授权后，按任务2138181撤回旧015灰度，requestId=`0614d553-a585-4996-aa15-d44e99bd8941`；012继续默认，015保留未删除。
- 重新提交成功，requestId=`e1ad384d-5929-488a-acf9-524c23853413`；候选016 buildId=2603246768、runId=`multi_tenant_1x5lHVN49gZBp3`，最终状态normal。脚本部署后比较确认网络配置未变。
- 发布单2633098使用URL_PARAMS，`gray015=1`定向到016，012保持默认；ReleaseGray requestId=`b749eea0-66ca-4720-8e6d-e69ee9282b4c`。沿用客户端既有开发版白名单，无需临时改客户端参数。
- 公网服务域名首次探针冷启动超时，第二次返回404 NOT_FOUND；源码路由本地测试确认按pathname处理查询参数，但公网域名不能替代小程序私有callContainer灰度验收，因此不把该结果记为能力端点失败或成功。
- 当前工作树微信预览构建成功：主包1451776字节、总包17011171字节，二维码`/tmp/wepic-016-preview-ytcvUp/preview.png`。下一步为真机验证新建空白卡、样式编辑、重排、当前页与整组保存；未全量切流、未合并main、未部署processOutfit。README无需更新：同服务定向候选部署不改变正式入口与用户运行方式。

## 发布前代码审查与本地修复

- 独立审查AI、卡片、发布配置；修复审核暂不可用误删AI原图、纯色卡改配色/背景文字低对比、持久备份无法恢复、实验故事缺审核SDK却放行。均先复现失败再补修复，保留违规拦截和内容。
- 自由卡保存从固定门禁改为同服务/灰度的鉴权POST能力查询，新服务明确protocol=1/freeCards=true才请求最终保存，旧服务继续阻断；没有改云端路由、全局开启能力或绕过审核。新增旧端点/坏响应/鉴权回归，更新缺服务配置断言。
- 六枚Tab图片完整CRC和解压有效，实际破图根因仍待运行包核验；新增图标资源预检。两项目入口显式排除cloudhosting，未宣称真实上传清单已核验。
- 最后全量测试的预检子进程曾卡住；sample堆栈定位Environment::Exit等待V8Worker退出，改process.exitCode并避免误打印成功后重跑。实际452/452、95文件语法、普通/声明call-container-only的release预检、TypeScript全部通过；日志/tmp/wepic-review-final-tests.log。静态声明不证明线上网络安全。
- README 已更新：新能力查询及部署交接。审查报告docs/deployment/2026-09-13-review-handoff.md列已修复与剩余项，源码zip已生成且校验SHA256；本机无Docker命令，未做Linux镜像构建。未合并、提交、部署、切流或操作微信平台。实验故事独立包依赖、真实云端保存、图标运行态及资质仍未验收。

## 上线范围重新规划

- 用户中断保存修复，要求先给现有功能上线计划；仅只读检查与规划，未调用部署脚本。发现旧部署脚本默认FULL且修改审核变量，不适合直接用于本轮候选升级。
- 新增release-readiness计划，分开本地实现、真实部署和微信资质；核验CloudBase官方AI资质说明，账号主体和类目仍需后台证据。
- README 无需更新：本轮只新增待确认计划，不改变运行方式或已实现能力。未重跑业务测试，不把历史439通过当成本轮验证。

## 混元简短AI水印配置

- 用户确认采用“AI”标识，保留小程序动态编号；设计与计划已更新。hunyuanImage请求增加官方footnote字段并补参数断言，未添加关闭水印参数、未改变模型/Key/重试通道。
- 实跑439/439测试、95文件语法、小程序预检、类型、文档治理和git diff --check全部通过；测试日志/tmp/wepic-watermark-tests.log。
- README 已更新：参数配置、重新部署processOutfit及仅新图生效的边界；同步部署说明与current。未部署、未调用真实模型，不消耗模型额度，实际水印外观仍待部署后单图验证。旧图不追溯改写。

## 分组尾卡加号实施

- 用户确认分类语义，更新设计并写计划；新增184rpx尾卡加号，空组共用菜单。复用手动指定组和AI自动分类，不清空用户素材；满组/保存成图保护、取消无副作用。
- 用户提出编号替代水印；指出烘焙编号无法随排序更新，异步询问短AI文字方案。官方当前API未查到关闭能力，不盲传LogoAdd或空字符串，未修改水印/调用模型。
- 新增菜单路由、空满组、状态保护、尾卡结构测试。首轮旧空组绑定断言失败，按已确认双选入口更新；最终439/439、95文件语法、小程序预检、类型、文档及git diff --check通过，日志/tmp/wepic-group-add-tests.log。未部署/真机验证；此前空白卡修复回归通过，云端自由卡保存门禁仍保留。
- README 已更新：加号入口、客户端更新与水印待确认边界。

## 空白卡风格/首次改字bug修复

- 根因：insertFreeCard继承source.paletteKey但使用candidate.stylePackId；updateCardText首次创建文字层仍以candidate风格重组，传入场景chalk-board-green造成报错。分别统一使用source/scene风格，不放宽白名单。
- 编辑同步及导出对已存在的纯色自由卡进行窄范围兼容：仅配色与风格错配且配色唯一归属于已知风格时修正stylePackId，不修改原输入；保留背景、笔迹及卡片顺序。占位文案改“（点击添加文字）”。
- 新增绿色黑板空白卡首次改字、再插卡、7张有序导出/预览模型、旧草稿无突变兼容回归。436/436、95文件语法、小程序预检、TypeScript、文档检查通过；无真实云端请求或部署。真机和相册保存仍待验证，旧服务自由卡能力门禁仍关闭。
- README 无需更新：本轮修复已确认的局部风格与自由卡行为，不新增产品范围或部署方式。

## 分组加号入口与水印调查

- 已核对dressup横向素材条、手动添加与AI导入/合并接口，形成group-add-entry待确认设计；AI是否保留自动分类是需确认的用户行为差异，未修改代码。
- 检索本地成图/合成路径未发现“AI生成”绘制，官方CloudBase当前I2I支持footnote；其他API的LogoAdd不能证明当前接口支持关闭。腾讯FAQ要求AI生成标识。没有修改水印或调用真实模型。
- README 无需更新：本轮仅设计及只读调查；npm run check:docs与git diff --check通过。

## AI真实进度与错误提示修复

- 用户确认后，更新设计并写实施计划；新增outfitProgress队列与页面接入：逐图并发2、真实完成/失败数、状态缩略列表及耗时。全部成功自动按原顺序归组；部分失败留在本页，显式仅重试失败或仅加入成功结果；审核拒绝禁止重试同一源文件。取消/离页停止排队与旧响应回填，不宣称停止后台计费。
- 不再通过cloud.callFunction前缀判断权限；区分超时、权限、网络、审核与生成失败。status:done但图片processing_failed不可算成功；上传失败不静默过滤。成功结果只保留会话内，不新增数据库或后台恢复，不改云函数或模型。
- 验证：定向16/16；全量434/434、95个首方JS语法、小程序预检、TypeScript、文档治理通过；git diff --check本次已能执行并通过。新增9图乱序/并发、分类其他组、失败重试去重、取消、审核拒绝及页面部分失败回归。预检仍有既有发布门禁提醒。
- README 已更新：真实进度、会话保留与重新编译边界。未部署或调用真实AI，未做真机9图/小屏视觉验收，不能声称线上超时已解决；需要当前工作树更新预览包后单图再多图验证。

## 内容区手写与新卡实施

- 最终实际检查：411/411、90文件语法、小程序普通预检、TypeScript、文档及diff全部通过。预检仍提醒生产入口call-container-only及公网独立验收，不代表生产门禁通过。
- 最终开发预览码 `/tmp/wepic-handwriting-ui-jNU9p0/preview.png` 构建成功，主包1427330字节、总包16986725字节。未提交审核、未发布、未部署后端。手机界面与新协议线上成图仍未验证；当前新卡保存被兼容门禁阻止，不称可上线。

- 用户确认后新增逐文件实施计划，迁移手写/草稿到文字下、字号前；两种用途、新卡背景色、空白卡入口、内容内手写编辑/删除。保留既有变换手势；手写面板背景和笔迹一起撤销/重做。
- 新卡稳定sceneId/独立基线，支持新增/删除/再次新增/排序/恢复和最多8张；背景为显式solid协议。旧草稿默认透明，带背景草稿可改作透明贴纸，副本不联动原草稿。localOnly画布原先总清成透明，现按场景背景绘制以支持背景预览。
- 服务端增加严格solid背景校验，渲染/笔迹图片审核仍复用原实现。未部署时能力声明false，新卡/solid保存提前说明待更新，空卡另给提示；不触发旧服务审核。README 已更新：说明新能力和部署兼容门禁。
- 首轮75个已有定向测试通过；新增9项通过、全量410/410及语法/预检/TypeScript/文档检查通过。之后补充背景撤销和本地/服务器PNG像素对照；最终结果补记下方。
- 开发者工具auto启动成功，可连接并读取首页，但navigateTo/reLaunch均返回Uncaught [object Object]，未得到新界面截图，不宣称视觉验收通过。CLI preview第一次构建成功（主包1426712/总包16986107字节）；后续代码细化后需重建最终预览码。不调用真实审核或相册保存，真机仍待人工。

## 预览保存范围纠正为整组

- CLI preview成功：主包1417635字节、总包16977030字节，二维码`/tmp/wepic-group-preview-89n70a/preview.png`。本轮自动化样例导航/注入返回空异常对象，未取得新的截图；按钮间距及真实整组相册保存待人工复核，不引用旧截图作本次视觉通过证据。

- 按用户明确纠正更新设计及实施步骤：不再保存选中单图，使用已审核manifest项目顺序逐张保存；移除当前张数/展开点图提示，占位消失后按钮下移，保留16px底部间距。
- 先将旧单张断言改为整组，观察2个失败；实现后添加真实回调模拟的第N/总数进度和失败续存测试。最终401/401、90文件语法、小程序预检、TypeScript、文档和diff检查通过。
- 审核失败零写入、重复点击锁、缓存复用及离页保护保留；成功后再次保存从头开始，部分失败同会话从失败位置继续。保存调用顺序不等于系统相册显示顺序保证。
- README 已更新：说明预览页默认保存整组，取代之前单张语义。未部署、切流或正式发布；1～2张后端限制仍待部署。

## 微信预览图标与原头像修正

- 新CLI开发预览成功：主包1417829字节、总包16977224字节，二维码`/tmp/wepic-icons-preview-KsCgE3/preview.png`，未上传审核版。

- 用户三处视觉反馈按纯视觉修正执行，不改变保存/审核行为。官方Lucide检索并采用sun/moon/download的24×24矢量路径，附ISC/Feather MIT声明；移除字符图标和中间竖线，统一尺寸与居中，保留整体点击和200ms滑块。
- 使用用户提供的`codex-clipboard-98b4839d-88f9-4566-acab-cfed9d4fd4e2.png`头像，资源缩至192px以控制主包体积，CSS裁去外围空白，不再使用拼画猫咪。保存按钮采用下载底托图标、600字重和12px间距。
- 新增本地素材/24画框/无字符竖线回归；400/400、90文件语法、小程序预检、TypeScript、文档和diff检查通过。
- 实际开发者工具导航并注入内置样例，只检查布局：`/tmp/wepic-icons-preview-KsCgE3/light.png`及`dark.png`均已查看，猫咪素材、图标、开关左右位置和保存按钮可见。未点击真实审核或保存，不作为真机相册验收。
- README 无需更新：纯视觉素材修正，不改变功能、入口及运行方式。未部署、切流或发布。

## 已确认的微信预览改版实现

- 排序按钮等比正方形、去除默认button间距、按缩略宽度计算区域高度；预览小鹿居中、左侧整体主题开关及200ms滑块、猫咪CSS插画和12px头像间距。不是截图原素材逐像素复制。
- 两个入口传递项目快照，预览保存按当前URL对应稳定cardId选择审核成品，继续整组审核只保存一张；未审核预览仍禁止原生长按保存。黑白等待转圈及真实回调阶段显示，拒绝/异常不保存、同指纹复用、重复点击锁、离页停止后续保存。
- 新增4个保存回归：当前目标/缓存复用、审核失败零写入、并发与退出保护、展开目标选择。首轮新测试因测试URL不合法失败，改为真实允许的wxfile协议；旧标题好友断言按需求改小鹿。最终399/399及90文件语法、小程序预检、TypeScript、文档和diff检查通过。
- 开发者工具实际进入预览页成功，截图`/tmp/wepic-preview-refresh-GdckrK/screen.png`核对居中标题、左侧开关与导航避让；空输入页面截图不算卡叠/头像/保存视觉验收。随后样例注入失败，本轮没有真实审核、相册保存或双端验收。
- CLI preview构建成功：主包1376866、总包16936261字节，二维码`/tmp/wepic-preview-refresh-GdckrK/preview.png`。未部署、未切流、未发布；1～2张服务端限制仍存在。
- README 已更新：补充预览保存入口与样式改版及未发布边界。

## 微信预览视觉与保存入口设计

- 本轮重新执行395/395测试、90文件语法、小程序预检、TypeScript、文档检查及git diff --check通过；只验证既有代码，不代表新设计已经实现。

- 根据三张截图核对共享预览页：标题好友、emoji头像、开关靠右；previewOnly当前禁止保存，没有可直接审核的项目快照入口。
- 新增待确认设计`2026-09-13-preview-save-refresh.md`，拟保存当前顶层单图并以sceneId绑定审核成品；仅保存触发审核，等待真实阶段动态显示，其他玩法不扩展审核。
- 本轮仅设计，无业务代码、云端和流量变化。README 无需更新：待确认需求尚未实现。

## 新开发预览包交接

- 重新实跑395/395、90文件语法、小程序预检、TypeScript、文档及diff检查通过。
- 开发者工具可连接并读取首页，自动导航编辑页返回`Uncaught [object Object]`，本轮视觉检查未完成。
- CLI preview成功：主包1369486字节、总包16928881字节，二维码`/tmp/wepic-save-only-preview-eOEMfU/preview.png`。待人工检查按钮排序和免审核预览。
- 未部署、切流或正式发布；1～2张保存仍待服务端获批部署。README 无需更新：本轮仅重新验证与生成开发预览包，无业务改动。

## 保存审核分离与显式卡片操作（用户确认后实施）

- 最终实际验证：395/395通过；check:syntax（90文件）、check:miniprogram、lint（tsc）、check:docs、git diff --check全部通过。预检仍提醒上线前独立核验call-container-only和公网入口；不是生产放行。

- 先写设计/计划及失败测试，观察旧微信预览/结果页会审核、缺少按钮方法及少于3张失败；随后实现。基线394/394通过。
- 卡片条普通view横向浏览，前移/后移/删除、sceneId维持选择、200ms过渡，删除进撤销历史，保留1张。已有手写贴纸与文字随场景保存。
- 新funLocalPreview共用本机字体轮廓/scenePainter及完整笔迹合成，结果页进入和微信预览零审核；独立保存入口调用最终审核，失败不得保存。同会话内容不变复用审核结果，历史预览不可用作通过依据；过期异步任务与重复保存受保护。
- 微信预览自带的长按保存/原生图片预览菜单对previewOnly模式禁用并提示返回保存入口，其他玩法不改。预览进度不再谎称审核通过。
- 发现删除到1～2张与原服务器3～8张规则冲突，按已确认至少1张范围修改本地sceneValidator的最终成图最小值，候选预览规则仍3张；线上015未部署该变化，本轮不发云请求、不切流。
- 删除11项已不成立的“进入结果页云渲染/网络兜底/旧云缓存复用”测试及旧拖动测试，替换为保存专用安全边界、竞争请求、历史不能跳过审核、本机PNG笔迹像素等回归。保留历史身份隔离、编号、断点续存与审核错误测试；测试数量变化不是隐藏失败。
- README 已更新：明确当前预览/保存边界和1～2张需要后端部署。真机滑动动效、字体/贴纸视觉、微信预览和相册新流程尚未实测，不把单元测试当作真机通过；未合并/提交审核/正式发布。

## 10:20真机callContainer 102002只读排查

- 截图102002不是WECHAT_AUDIT_45009；缺少该次客户端trace和请求起止时间。
- CLS 10:15–10:24，requestId=1876768f-5816-42bf-bce0-15748e1bc1fb，ListOver=true，仅两条：015 pod fun-card-renderer-015-746664786c-dx4jv于10:21:03监听8080；012 pod fun-card-renderer-012-df5dc4647-wmk5b于10:21:05监听8080。无审核/渲染阶段记录。复查requestId=dc9d6636-131c-4284-90d2-6b6741d25f24结果相同。
- 发布单requestId=bb141906-b93b-4ff0-af48-08b6f62165ca：012/015 normal，URL_PARAMS gray015=1，最小实例均0。启动时序支持冷启动假设，但不能证明截图请求命中或唯一根因。
- 未改业务代码、流量、实例、微信配置或额度，未发新的审核请求。建议人工单次重试取证，不能将此视为长期修复；常驻实例涉及费用，未擅自开启。
- README 无需更新：仅诊断记录，无功能/运行方式变更。本轮不重跑业务测试，不将之前394/394视为本轮结果。
- 本轮实际验证：check:docs与git diff --check通过。

## 015开发版真机入口准备

- 用户确认继续定向真机步骤，补充实施计划并在env.js配置FUN_CARD_RENDERER_DEV_GRAY=gray015。共享客户端只在最终render-stack、固定白名单配置、运行时develop三条件满足时加参数；trial/release/未知版本/API异常保持默认，清空配置可恢复开发版默认。未改安全策略、重试、云端资源或默认流量。
- 新增7项环境隔离及配置白名单回归，394/394通过，check:syntax（89文件）、check:miniprogram、lint、check:docs及diff检查通过。开发者工具9442实时读取envVersion=develop；生产入口独立核验提醒仍保留。
- 只读发布单requestId=d71a0b31-ecdd-40b3-8fcd-bf63e5a95679：012默认，015 normal，URL_PARAMS gray015=1。此证据不代表手机请求已命中。
- 通过微信CLI preview构建开发预览包，不调用upload正式版本/submit审核/release发布。本机二维码目标/tmp/wepic-015-preview-5wzgmU/preview.png，结果待追加；不会替用户执行真机相册验收。
- README 已更新：补充开发版定向测试配置、正式/体验环境隔离和清空配置恢复方式。
- CLI preview实际成功，主包1366048字节，总包16925443字节；已生成并查看二维码。等待用户用本次开发预览码扫码，验证先保存再编辑的当前页/整组保存，并提供时间与截图用于核对015日志。未宣称手机端已通过，也未正式发布。

## 保存修复015部署与真实图片对照

- 用户允许下一步候选部署。确认014任务2138006仍running/gray后，OperateServerManage go_back撤回测试路由，requestId=5a6e3277-3296-41f2-86e9-f6aaeca4dc22；复核012 normal/100%，013/014 normal/0%，未删除旧版本。
- 白名单源码包7347271字节，SHA-256=821289e6e0c71145836c073931c7e433b19e653c7cef9050cdd45223f8568cbf，不含环境文件。首次JSON字符串配置比较不相等而保护性停止，未提交部署；该次差异未保留，不能断言具体原因。再次按键排序比较changedKeys=[]才提交。仅DeployInfo/GRAY，无Items或配置字段；requestId=6bd68180-9cb1-4110-a5d6-fb4bfc273476，TaskId=2138181；提交前后配置规范化比较完全一致。
- 015 buildId=2602672211，runId=multi_tenant_1x5RTSXAn7Sra0，01:25:10开始、01:27:39构建完成normal，发布单2627336。012/015比较仅Name、CreatedTime、UpdatedTime、构建状态与RequestId不同，运行字段相同；015详情requestId=9f091063-b940-49c4-a45b-1dbc80226a78。
- ReleaseGray配置URL_PARAMS，015 gray015=1/priority1，012默认/priority2；requestId=6a202cf5-69fc-4a52-b2b4-81863c293ee5。复核requestId=207cef85-c7d7-48c9-b944-358e47b45b3a，GrayStatus success，CurrentVersion012、ReleaseVersion015 normal。没有全量推广。
- 开发者工具已登录；最初miniprogram目录自动化连接失败，重新以仓库根配置打开后9442恢复。真实wx.cloud.callContainer测试项目funtext_savecheck_1789234116979，两次请求均200，各3张；先无新增对象，再首卡加星星与紫/蓝两条固定测试笔迹。两次输出revision分别da5fa171-bfa5-463a-870b-34246274d0b6、2c94ac51-3a94-4d19-bd77-766ab34fd256，未覆盖相同地址。
- CLS窗口01:27–01:30，requestId=e950a26e-7563-4116-b596-29a95cabe421，ListOver=true共7条，均015实例fun-card-renderer-015-746664786c-zxdsp：8080启动1条、两个请求各3条stage日志；每次audit-start仅1次，render-end均200，耗时2085ms/1663ms。traceId分别63cdef77-aecf-11f1-9af3-525400144582及65ad0c42-aecf-11f1-862b-5254005a2290。本窗口没有5xx；不代表未来无45009。成功请求不能验证失败errmsg/rid字段。
- 真实云文件下载并解码首卡：编辑前后都是1080×1080 PNG。旧图两个采样点均[252,228,236,255]；新图为紫[118,87,255,255]、蓝[32,119,212,255]，星星区域哈希变化。两文件SHA-256分别4e938ea461f1e4d5dff8a06a7f0ffffbe75b5051fb966160ba97d4588e722683、bfbbf1e05b2414324d7e452ff40ce2d0e283ed22ac6178b47e8648d824cf1330。这是固定测试数据云端生成/下载证据，不是用户手机相册验收；未写入用户相册。
- 重跑387/387、check:syntax（89文件）、check:miniprogram、lint、check:docs、diff检查通过。生产私有入口仍需独立核验。未修改VPC、子网、套餐、AppID/AppSecret、审核规则，无clearQuota或小程序正式发布。
- 下一步为明确命中015的人工真机按钮保存验收；普通客户端不带gray015仍走012。自由创作新增功能尚未编码。README 无需更新：同服务候选bug修复部署与验证，不改变正式入口/运行方式；未全量发布或合并。

## 授权实施：保存一致性第一阶段

- 用户确认自由创作设计，新增逐阶段实施计划。先修保存缺陷，不同时叠加新场景协议或界面变动。
- server.js最终renderStack新增服务器生成的randomUUID revision，不复用客户端traceId，避免固定文件路径覆盖。沿用renderer已有revision路径和回滚机制；未修改审核规则、调用次数或云端配置。
- 新真实@napi-rs/canvas回归：同项目先生成原图，再添加星星与两条紫/蓝笔迹；模拟按URL下载缓存，确认新地址、旧PNG不变，新PNG颜色像素与星星区域变化。该证据验证修复机制，不冒称复现用户手机缓存行为。
- 新编辑页回归：手写贴纸进入最新payload并展平为strokes；编辑后重新生成，未编辑的当前页/整组/微信预览复用最新manifest。保存端没有另外丢弃手写字段。
- 重新执行387/387通过，check:syntax（89文件）、check:miniprogram、lint、check:docs、git diff --check通过。生产入口核验提醒仍存在。未部署、未切流、未clearQuota、未发布或合并。
- 下一步需将本地修复部署为新候选并定向真机对照保存，当前默认012及定向014不含此修复；自由创作新增界面与模型尚未实施。README 无需更新：当前仅存储版本隔离bug修复与已确认实施计划，无用户入口或运行方式变更。

## 真机保存缺失反馈与自由创作规划

- 用户两图确认新增星星及双色手写没有出现在相册成品；真实保存一致性未通过，之前三PNG链路成功不能替代这一验收。
- 静态核对编辑快照、完整payload指纹、inkStickers展平、manifest物化和服务端存储路径。确认最终请求未给revision而覆盖固定路径，属于需要修正的缓存/并发风险；尚未取得本次真实payload和原始成图，不声称已找到唯一原因或已修复。
- 新增待确认自由创作设计：现有三Tab内分布空白卡、背景、文字对象、手写添加与变换；先修保存一致性，再实施新增功能。README 无需更新：本轮仅排查与待确认设计，未改变运行方式或功能。
- 本轮不修改代码、云配置、路由或审核规则。重新实跑385/385通过，check:syntax（89文件）、check:miniprogram、lint、check:docs和git diff --check通过；预检仍要求生产私有入口独立核验。自动化通过不能否定本次真机缺陷。

## 后续授权的本地诊断修复

### 授权撤回013并创建014

- 用户允许撤回旧灰度。按官方traffic rollback对应的OperateServerManage go_back操作任务2134255，requestId=32161239-eba8-495c-ba72-af5a15302370；复核012 normal/100%、013 normal/0%，013未删除，旧发布单IsReleasing=false。
- 仅DeployInfo（GRAY）提交原服务新源码，不提交任何运行配置字段。UpdateCloudRunServer requestId=ad1739a8-3e0f-4102-80eb-9c9dc8f4477c，TaskId=2138006，新版本014。源码包SHA-256=f64e0ec49cdc7a4eac271c552d22d8e42de5d518e8c1b30670cac49c9f07668a，7347182字节；提交前后ServerConfig完全一致。
- 012与014的版本配置比较：CPU1/内存2/8080/副本0–5，以及EnvParams、VpcConf、PublicNetConf、PolicyDetails、VolumesConf、Cmd、EntryPoint、Dockerfile、BuildDir等相同；创建/更新时间和构建中状态不同。requestIds=06a6db60-ff22-47c0-ace3-6104ce13c614、820ebc3e-dca1-4cf7-89ec-6173d8ad044f。
- 本轮重新实跑385/385及治理全部检查通过；微信开发者工具登录与自动化连接成功。构建/定向真实验收结果待下方追加，不将构建中视为成功。README 无需更新：诊断修复的同服务候选部署，不改变用户功能或调用方式。

### 014构建及定向真实验收结果

- 014云端构建成功并normal：buildId=2602667478、TaskId=2138006、发布单2627142；源码为fe13685基线加本地已验证诊断改动，并非新的已提交Git版本。本轮没有合并、推送或正式发布小程序。
- 初始URL_PARAMS规则提交requestId=18f71e56-485f-4983-889e-52f618ca4ec3。首个HTTP200请求通过CLS发现实际命中012，明确不计入014验收。随后将014显式gray014=1规则放前，012为默认回落（gray014=0），requestId=d791758c-f218-4508-8c8a-e2b05b2d8961；不能仅凭时间关联断言首个错路由一定是优先级而非传播延迟所致。
- 最终发布单读取requestId=441d0031-5345-428b-ad8e-705ce3ba2caf：CurrentVersion=012 normal，ReleaseVersion=014 normal，TrafficType=URL_PARAMS，gray014=1，GrayStatus=success。012默认，014仅定向测试；URL模式不能将API返回的比例0误读为服务停流。013仍保留normal，未删除。
- 真实wx.cloud.callContainer /render-stack?gray013=1不是本轮目标；本轮成功目标为/render-stack?gray014=1，客户端开始1789231874594、结束1789231877608，HTTP200/ok=true/3张。固定文字“春天你好”，三字体marker/playful/headline，第三张带一条固定蓝色笔迹。最终流水线成功说明文本及笔迹图片审核门禁通过，但未抓取原始微信成功响应，未断言token实际刷新次数。
- CLS确认014实例fun-card-renderer-014-5d4c97b44b-jxg2x：00:44:44 listening on 8080；00:51:16 audit-start kind=final；00:51:17 render-start elapsedMs=532；00:51:18 render-end kind=final status=200 elapsedMs=2039。三个业务阶段均带traceId=2b0196d4-aeca-11f1-88d4-5254006937e9及时间；audit-start仅一次。
- CLS查询00:42–00:53，ListOver=true、共8条，其中014四条；requestId=e10c1b12-749c-4834-8f70-8065ebec582c。本窗口没有014的5xx、错误或敏感字段正则命中，不推断长期无异常。未记录token、AppSecret、openid或完整用户正文。
- 014返回的三张文件经wx.cloud.downloadFile和wx.getImageInfo验证均PNG、1080×1080。字体启动注册和三字体成图链路正常，但实际字形视觉、双端手势、相册授权与保存进度仍需人工真机检查；没有代替人工验收。
- 新traceId成功日志线上验证通过；errmsg/rid仅失败分支产生，本轮成功请求未验证这两个失败字段，证据仅来自本地mock测试。不人为触发配额故障，不clearQuota。
- 最终结论：可以进入014定向人工真机功能测试；不全量切流。保留012默认回滚及013旧候选，没有改VPC/子网/套餐/微信配置或开启新收费服务。文档更新后重新执行check:docs与git diff --check。

### 先前部署被阻止的历史记录

- 重跑385/385，check:syntax、check:miniprogram、lint、check:docs、git diff --check通过；微信开发者工具islogin=true。现有脚本默认FULL且会覆盖微信变量，因此本轮使用仅DeployInfo的API请求，不提交Items/ServerConfig。
- 源码白名单压缩包7347182字节，SHA-256=f9c4613723a5be3091ecd1ace07c3df0ee2ab19c0deda14e14ca067c23454b12，已上传构建源；不含.env或本机配置。UpdateCloudRunServer显式ReleaseType=GRAY被ResourceInUse拒绝：当前已有部署发布任务运行中，请结束后再执行；requestId=6a952fe2-d665-446c-99e5-32d722683cf6。未创建新版本，不声称云端构建成功。
- DescribeServerManageTask：2134255，running，GRAY，PreVersionName=012，VersionName=013，ReleaseId=2623217；GrayRelease步骤仍running。未调用回滚/结束/删除接口，需先确认旧灰度撤回的影响与授权；未改普通路由，无真实新版本调用可验收。
- README 无需更新：部署尝试被拒绝，仅记录证据，不改变既有运行方式。

- 修复最终审核/成图阶段traceId缺失、图片审核缺少traceId和SHA-256；保留HTTP/API失败errmsg/rid/status及时间，网络错误不输出原始异常URL。并发token失败按调用者关联，45009不自动重试，审核规则不变。
- 新增最终调用链关联、图片45009 HTTP/API诊断、并发token失败、异常脱敏测试。首轮385项中383通过，2项旧断言仅因新增traceId/context副本失败；更新断言验证身份仍来自可信context、调用者原对象不变。
- 本轮未部署、未切流、未clearQuota；012/013线上仍是旧镜像。README 无需更新：仅补齐诊断，无用户流程或运行方式变化。
- 最终实跑：385/385测试通过，check:syntax（89个首方文件）、check:miniprogram、lint（tsc --noEmit）、check:docs及git diff --check全部通过。预检仍提示生产call-container-only及关闭公网入口需要独立核验；本地通过不代表新日志已在线上验证。

- 仅只读检查与记录证据，不改业务代码、不部署、不切流、不clearQuota、不发布、不合并。
- DescribeReleaseOrder requestId=2963c6b4-82d0-4465-8527-c58db188a476：CurrentVersion=012 normal、ReleaseVersion=013 normal，URL_PARAMS，gray013=1。
- CLS查询2026-09-12 23:06:47至2026-09-13 00:06:47，100条分页上限，ListOver=true、总计0条。无流量证据不能用于声称持续稳定、无重启或无故障；上轮23:06:15成功请求不在此窗口内。
- 静态核对server.js生成并传递traceId，但audit-start/render-start/render-end阶段logStage只输出kind、status及耗时；成功阶段缺少关联ID。wechatSafety.js图片审核HTTP/API错误分支traceId/contentHash固定为null。文本审核错误已有相关元数据；本轮没有人为触发微信审核失败，线上errmsg/rid仍未实测。
- 开发者工具自动化仍可连接，但getApp()._accept013测试会话不存在，无法重读上一轮三张图片。未重新审核或改动用户作品，字体视觉/双端手势/相册继续由人工验收。
- 重新执行npm test：381/381；check:syntax（89文件）、check:miniprogram、lint、check:docs通过。普通预检仍提醒生产私有入口核验。README 无需更新：仅新增验收证据，无业务/运行方式变更。
- 结论：可以继续人工真机功能测试；完整上线验收未完成。若要达到完整诊断目标，需另行授权小范围日志修正并部署新不可变版本，不能声称013已覆盖全部日志要求。
# 内容区手写与手写新卡设计

- 只读核对编辑WXML、手写处理、handwritingLibrary及项目模型；现有透明手写贴纸可添加/变换，草稿只保存笔迹与视口。没有将背景手写新卡能力当作已实现。
- 新增待确认设计 `2026-09-13-handwriting-content-cards.md`，修订旧方案装饰入口为内容入口；确认后再写实施计划及编码。
- 本轮仅文档，未改代码、部署或流量。README 无需更新：新行为尚未实现。实际执行 `npm run check:docs` 与 `git diff --check` 通过；未重跑代码测试，不引用历史结果作为本轮验收。
# 新截图需求设计：保存发送与AI归组

- 只读核对 processOutfit、outfit-import、主预览绑定与共享编号；新增待确认设计 `../superpowers/specs/2026-09-13-save-send-ai-preview.md`。
- 微信官方类型资料确认 exitMiniProgram 仅退出小程序，不将退出描述为自动进入指定聊天/发送多图。
- 未改产品代码，未调用付费AI或部署；README 无需更新：本轮仅设计，现有功能范围未变。
- 验证：npm run check:docs 与 git diff --check 实际通过；未重跑产品测试，不引用历史测试为本轮通过。
# 已确认需求本地实施：AI标准构图、发送与浏览

- 用户确认上一设计，补充尽可能还原上传款式和生成后统一大小。计划见 `../superpowers/plans/2026-09-13-save-send-ai-preview.md`。
- processOutfit 分类别生成提示词并增加头像分类、处理失败状态；不换模型/配置。导入页原先未经过 cardComposer，本次接入实际白边检测与92%等比构图，默认3:4；纯白/读取失败保守回退不盲裁。生成文件保存本机后直接回填，其他/待确认分开，已有素材不覆盖。
- 工作台增加其他素材组、AI素材改分类/原图操作；普通四部位计数不含其他组，至少3张门槛未改。试玩转AI经用户确认才清空示例。
- 保存成功引导可退出当前小程序，但不保证指定聊天/自动发送；趣味字画自定义大图不带原生长按保存菜单，主画布非绘图区转发横滑事件，保留对象手势优先。角标v2中灰白字居中。
- 新增真实Canvas像素与状态回归。实际AI质量、真机滑动/返回微信/相册未验证，云端未部署、不切流、不clearQuota。
- README 已更新：说明本地候选功能与未部署边界。
- 验证：最终419/419通过（含真实Canvas构图、规范化图与导出URL一致性）；93个JS语法、小程序预检、TypeScript、文档治理与git diff --check通过，测试日志 `/tmp/wepic-save-send-final.log`。预检仍提醒生产私密入口门禁未完成，未进行真实AI调用或真机验收。
# 其他改动补齐复核

## AI等待进度设计

- 检查processOutfit批次日志及outfit-import整批callFunction/省略号定时器。新增待确认逐图有限并发方案，真实回调计数、失败分开、取消不承诺撤销已发出的模型请求。
- README 无需更新：本轮仅待确认设计，未改代码、模型调用、数据库或云端配置。文档检查结果另行记录，不引用历史测试为当前验收。

## 已确认空穿搭/作用域实施

- 用户确认设计并明确其他素材只在AI结果存在后显示。新增实施计划，upload入口不自动读取mixed草稿，提供显式恢复且写入新草稿前备份旧草稿。不清除其他项目素材。
- 样式范围移到面板顶部；新增updateStylePack保留场景身份、顺序、文字和手写，不重建模板；当前场景驱动样式控件。整组背景/配色在不同风格中应用相应选项序号，不改变其他设置；纯色背景和字感直接统一。
- 移除纯背景空卡的EMPTY_CARD阻断，保留FREE_CARDS_NOT_READY及新增单卡独立风格的旧服务器保护。服务器按各场景白名单校验风格及背景，不跳过内容安全。线上服务未部署，能力未打开，保存尚待部署验收。
- 补七张新增/换位/局部与整组风格/预览顺序、mixed草稿空入口及恢复测试。首轮424/426，两个旧测试预期默认整组风格及旧文案；更新对应预期后428/428通过，94文件语法、小程序预检、TypeScript、文档治理通过。日志 /tmp/wepic-scope-final.log；git diff --check因xcode-select找不到开发工具无法执行，未安装系统工具。真机及云端未验收。
- README 已更新：入口、样式范围与云端未部署边界。不合并、不推送、不切流、不新增云环境；仍需现有renderer更新并验证后启用协议。

## 新截图：空项目与样式作用域设计

- 只读确认 upload 自动恢复mixed草稿、风格调用不读取editScope，以及FREE_CARDS_NOT_READY/EMPTY_CARD两个保存门禁。完成待确认设计 empty-outfit-scoped-style，不修改业务代码、云资源或能力开关。
- README 无需更新：本轮仅形成待确认设计，未改变运行行为。
- 验证结果：文档治理检查通过；git diff --check 因系统 xcode-select 未找到开发工具而未能执行。不引用历史426项为本轮业务验收。

## 后续：千问分类与混元成图接入

- 用户确认接入混元并要求提交步骤；规格/计划为 2026-09-13-hunyuan-outfit。当前工作树创建 codex/hunyuan-outfit 分支，保留继承的所有未提交改动。
- 成图改为官方 cloud.ai SDK 固定混元I2I，移除阿里云成图出口及应用层生成重试；缺分类Key明确失败，前端给出配置提示。保留审核、转存和既有构图。依赖安装得到 wx-server-sdk4.0.2 / @cloudbase/ai2.30.0，新增云函数锁文件。
- README 已更新：模型分工、未部署边界和部署文档入口。current 更新当前工作树；旧抠图文档标注历史请求不可用于新部署。
- 首轮426项有8项失败，原因是新工作树缺renderer Canvas依赖；安装已有锁定依赖后重跑426/426全部通过。94文件语法、小程序预检、TypeScript、文档治理和git diff --check通过；日志 /tmp/wepic-hunyuan-final.log。预检仍提醒生产入口门禁。
- 未部署、未真实调用模型、未核验账单抵扣。90秒和客户端整批同步等待期限不能保证，须单图后多图真机验收；若客户端先超时，后续需异步任务改造。未修改网络/renderer路由/套餐，也未宣称可提交生产审核。

- 已逐项检查已确认的保存发送、AI直回/其他素材、大图浏览和浅灰编号，不再仅引用上轮测试数量。
- 修复预览保存失败时 saveDone=true 导致错误展示“去微信发送”；为旧预览保存成功路径补发送引导。失败不可显示成功或退出入口。
- AI每批imageId及服务端resultId改为批次隔离，避免多批都叫image_1/result_1造成误去重/重复UI key；取消/离开后的响应不允许回填新批次。
- 备用主图/旧Canvas补点击放大；大图重开使用generation及串行共享Canvas，旧结果不可覆盖新预览，随后保存等待旧Canvas任务结束。
- 定向16/16实际通过；最终重新执行全量421/421、93文件语法、小程序预检、TypeScript、文档及diff检查全部通过（测试日志 `/tmp/wepic-followup-final.log`）。预检仍提醒生产入口与公网关闭需独立验证；未部署、未更改路由、未调用付费AI、未真机验收。
- README 无需更新：本轮仅补齐已说明功能的遗漏与并发bug，不新增功能范围。
