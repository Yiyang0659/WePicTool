# WePicTool 三 Tab 页重设计计划（2026-07-18）

## 目标
按 ui-reference 参考图 + 已验收的结果页设计语言，重设计首页 / 记录页 / 我的页，直接实现代码。

## 设计基调（与结果页一致的 token）
- 浅灰底 `#f6f6f8` + 白卡片（圆角 24rpx、轻投影、间距 24rpx）
- 唯一渐变 `#4f6bf5→#6a3df0` 仅用于主 CTA；次级强调用黑底 `#111`
- 徽标胶囊：绿 `#e6f9ee/#07a350`、灰 `#f2f2f4/#999`；提示卡淡渐变 `#eef3ff→#f4efff`
- emoji 作功能图标；分段控件 `#ececf0`；safe-area 处理

## 阶段划分

### 阶段 1 — 探索（已完成）
- UI 参考图 + 原型风格提炼（explore 子代理）
- PLAYBOOK/PRD 待上线功能清单提取（explore 子代理）
- 现有三页代码契约梳理（explore 子代理）

### 阶段 2 — 并行实现（3 个 coder 子代理，文件互不重叠）
1. **首页 pages/index**：保留 step home/confirm 全部逻辑契约；home 步骤改为「品牌区 + 穿搭叠图旗舰渐变主卡 + 玩法模板 2 列宫格（大字滑卡/剧情滑卡/盲盒抽卡/拼图揭秘/翻页动画/成套搭配/滑滑换装，灰态"即将上线"占位，数据驱动，点击 toast 敬请期待）+ 资料打包工具位 + 操作说明卡」；confirm 步骤套 token 重排。
2. **记录页 pages/record**：保留 `wepictool_records` 契约；新增按功能类型分类（顶部 tab：全部 + 数据中实际存在的类型；类型元数据数据驱动，无 type 字段默认穿搭叠图）；记录卡按类型展示不同摘要（穿搭：分组张数 tag；其他类型回退"一叠 N 张"）；空态改圆图标 + 渐变胶囊 CTA；统计条重设计；加回清空入口。
3. **我的页 pages/profile**：保留 storage keys / 弹窗 / 分享契约；用户白卡（渐变头像 + 复制 ID + "已生成 N 次"徽标，N 从记录读取）→ 玩法预告提示卡 → 「功能」「设置与支持」两组菜单行卡 → 底部版本。

### 阶段 3 — 校验
- 各代理运行 `node --check` + `node scripts/check-miniprogram.mjs`，修复自身文件相关问题。
- 主代理汇总结果。
